{
  "id": "remotes-collaboration",
  "title": "Remotes & Collaboration",
  "category": "Git",
  "subcategory": "Remotes",
  "language": "bash",
  "description": "<p>Distributed Git means everyone has a full copy of the repository. Remotes are the bridges between copies.</p><svg viewBox=\"0 0 500 160\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%;height:auto;display:block;margin:1.5rem 0\"><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0s\" fill=\"freeze\"/><rect x=\"175\" y=\"10\" width=\"150\" height=\"50\" rx=\"10\" fill=\"none\" stroke=\"#E95420\" stroke-width=\"2\"/><text x=\"250\" y=\"40\" text-anchor=\"middle\" fill=\"#E95420\" font-size=\"14\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">origin</text></g><line x1=\"200\" y1=\"60\" x2=\"100\" y2=\"95\" stroke=\"#C34113\" stroke-width=\"2\" stroke-dasharray=\"40\" stroke-dashoffset=\"40\"><animate attributeName=\"stroke-dashoffset\" from=\"40\" to=\"0\" dur=\"0.4s\" begin=\"0.4s\" fill=\"freeze\"/></line><line x1=\"250\" y1=\"60\" x2=\"250\" y2=\"95\" stroke=\"#C34113\" stroke-width=\"2\" stroke-dasharray=\"40\" stroke-dashoffset=\"40\"><animate attributeName=\"stroke-dashoffset\" from=\"40\" to=\"0\" dur=\"0.4s\" begin=\"0.4s\" fill=\"freeze\"/></line><line x1=\"300\" y1=\"60\" x2=\"400\" y2=\"95\" stroke=\"#C34113\" stroke-width=\"2\" stroke-dasharray=\"40\" stroke-dashoffset=\"40\"><animate attributeName=\"stroke-dashoffset\" from=\"40\" to=\"0\" dur=\"0.4s\" begin=\"0.4s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.7s\" fill=\"freeze\"/><rect x=\"30\" y=\"100\" width=\"140\" height=\"50\" rx=\"8\" fill=\"#C34113\"/><text x=\"100\" y=\"130\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"12\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Your Clone</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.7s\" fill=\"freeze\"/><rect x=\"180\" y=\"100\" width=\"140\" height=\"50\" rx=\"8\" fill=\"#238636\"/><text x=\"250\" y=\"130\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"12\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Clone B</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.7s\" fill=\"freeze\"/><rect x=\"330\" y=\"100\" width=\"140\" height=\"50\" rx=\"8\" fill=\"#F0883E\"/><text x=\"400\" y=\"130\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"12\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Clone C</text></g></svg>",
  "sections": [
    {
      "title": "Remote Repositories",
      "description": "<p>A remote is a bookmark — a URL plus a name (default: <code>origin</code>). You can have multiple remotes (origin, upstream, fork). Remotes are stored in <code>.git/config</code>. Use <code>git remote -v</code> to list them with URLs.</p><pre><code>git remote -v                   # list remotes\ngit remote add upstream &lt;url&gt;   # add upstream\ngit remote remove &lt;name&gt;        # remove remote\ngit remote rename &lt;old&gt; &lt;new&gt;</code></pre>"
    },
    {
      "title": "Cloning & Fetching",
      "description": "<p><code>git clone</code> performs an initial fetch, creates remote-tracking branches, and checks out the default branch. <code>git fetch</code> downloads new objects and refs without merging — safe to run anytime. Remote-tracking branches (<code>origin/main</code>) are read-only views of the remote.</p><pre><code>git clone &lt;url&gt;                # full clone\ngit clone --depth 1 &lt;url&gt;     # shallow clone\ngit fetch origin              # fetch all branches\ngit fetch origin main         # fetch single branch</code></pre>"
    },
    {
      "title": "Pushing & Pulling",
      "description": "<p><code>git push</code> uploads commits and updates remote refs. <code>git pull</code> runs <code>git fetch</code> then <code>git merge</code> (or rebase with <code>--rebase</code>). The upstream configuration determines the default push/pull target. Use <code>-u</code> to set upstream on first push.</p><pre><code>git push origin main           # push to main\ngit push -f origin feature     # force push (careful!)\ngit pull origin main           # fetch + merge\ngit pull --rebase              # fetch + rebase</code></pre>"
    },
    {
      "title": "Multiple Remotes",
      "description": "<p>Fork-based workflows use two remotes: <code>origin</code> (your fork) and <code>upstream</code> (original repo). Sync by fetching upstream and merging into your local main. Useful for open-source contributions.</p><pre><code>git remote add upstream &lt;url&gt;\ngit fetch upstream\ngit checkout main\ngit merge upstream/main\ngit push origin main</code></pre>"
    },
    {
      "title": "Troubleshooting",
      "description": "<p>Rejected pushes typically mean the remote has diverged. Use <code>git pull --rebase</code> to rebase your commits on top of the latest remote. Stale remote-tracking branches can be pruned with <code>git remote prune origin</code>.</p><pre><code>git fetch --prune origin       # prune stale refs\ngit pull --rebase              # rebase instead of merge\ngit push --force-with-lease    # safe force push</code></pre>"
    }
  ],
  "details": "<p>Remotes are stored as remote-tracking refs in <code>refs/remotes/&lt;name&gt;/&lt;branch&gt;</code>. These are distinct from local branches — they're only updated during fetch/push operations. The <code>protocols</code> supported are SSH, Git, HTTP/S, and local filesystem. Git uses packfile negotiation during fetch/push to minimize data transfer: the client tells the server what objects it has (via <code>have</code> lines), and the server sends only missing objects (via <code>want</code> lines). Refspecs (<code>+refs/heads/*:refs/remotes/origin/*</code>) control the mapping between remote and local refs.</p>",
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>git remote -v</code> — list remotes with URLs. Always check which remote you're pushing to.</li><li><code>git fetch --prune origin</code> — fetch + prune stale remote-tracking branches.</li><li><code>git push --force-with-lease</code> — safe force push. Rejects if remote has new commits you haven't seen.</li><li><code>git pull --rebase</code> — fetch + rebase instead of merge. Keeps history linear.</li></ul><h4>Staff+ Patterns</h4><ul><li>Set up <code>upstream</code> remote for fork workflows: <code>git remote add upstream &lt;parent&gt;</code>. Sync with <code>git fetch upstream && git rebase upstream/main</code>.</li><li>Use <code>--force-with-lease</code> over <code>--force</code> always. It checks that your local remote-tracking ref matches the server — a 3-way safety check.</li></ul><h4>Watch Outs</h4><ul><li>Force push rewrites history on the remote. Anyone who already pulled will have divergent history. Only force-push to private feature branches.</li><li><code>git push --delete origin branch</code> deletes the remote branch. Ensure no open PRs reference it.</li></ul><h4>Interview Angle</h4><p>GitHub L6 asks about Git protocol and packfile negotiation. Know the difference between <code>git fetch</code>, <code>git pull</code>, and <code>git pull --rebase</code>, and explain when each is appropriate. The want/have negotiation is a classic distributed systems pattern.</p>",
  "tags": [
    "git",
    "remotes",
    "collaboration"
  ],
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}
