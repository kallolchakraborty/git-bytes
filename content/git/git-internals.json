{
  "id": "git-internals",
  "title": "Git Internals",
  "category": "Git",
  "subcategory": "Internals",
  "language": "bash",
  "description": "<p>Git is fundamentally a content-addressable object store. Every file, directory, and commit is an object identified by its SHA-1 hash.</p><svg viewBox='0 0 800 300' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><defs><marker id='an-dn' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#E95420'/></marker></defs><text x='400' y='20' text-anchor='middle' fill='#E95420' font-size='15' font-weight='bold'>Git Object Graph</text><rect x='320' y='35' width='160' height='50' rx='8' fill='#F0883E'/><text x='400' y='55' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Tag Object</text><text x='400' y='72' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>tag v1.0 → commit</text><path d='M400 85 L400 105' stroke='#F0883E' stroke-width='2' marker-end='url(#an-dn)'/><rect x='300' y='110' width='200' height='50' rx='8' fill='#DA3633'/><text x='400' y='132' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Commit Object</text><text x='400' y='149' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>parent, tree, author, message</text><path d='M350 160 L350 180' stroke='#DA3633' stroke-width='2' marker-end='url(#an-dn)'/><text x='335' y='175' text-anchor='end' fill='var(--text-secondary)' font-size='9'>parent (0+ commits)</text><path d='M420 160 L420 180' stroke='#DA3633' stroke-width='2' marker-end='url(#an-dn)'/><text x='460' y='175' text-anchor='start' fill='var(--text-secondary)' font-size='9'>tree (1 per commit)</text><rect x='300' y='190' width='200' height='50' rx='8' fill='#238636'/><text x='400' y='212' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Tree Object</text><text x='400' y='229' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>directory listing: mode, name, SHA</text><path d='M320 240 L280 260' stroke='#238636' stroke-width='2' marker-end='url(#an-dn)'/><path d='M480 240 L520 260' stroke='#238636' stroke-width='2' marker-end='url(#an-dn)'/><rect x='180' y='260' width='100' height='30' rx='6' fill='#C34113'/><text x='230' y='279' text-anchor='middle' fill='#fff' font-size='10' font-weight='bold'>Blob: src/main.js</text><rect x='520' y='260' width='100' height='30' rx='6' fill='#C34113'/><text x='570' y='279' text-anchor='middle' fill='#fff' font-size='10' font-weight='bold'>Blob: README.md</text><text x='400' y='255' text-anchor='middle' fill='var(--text-secondary)' font-size='9'>Tree contains blob + subtree entries</text><rect x=\"0\" y=\"286\" width=\"800\" height=\"14\" fill=\"rgba(0,0,0,0.3)\" rx=\"0\" /><text x=\"796\" y=\"296\" text-anchor=\"end\" fill=\"rgba(255,255,255,0.5)\" font-size=\"8\" font-family=\"monospace\">git-bytes  © 2026</text><rect x=\"0\" y=\"286\" width=\"800\" height=\"14\" fill=\"rgba(0,0,0,0.3)\" rx=\"0\" /><text x=\"796\" y=\"296\" text-anchor=\"end\" fill=\"rgba(255,255,255,0.5)\" font-size=\"8\" font-family=\"monospace\">git-bytes  © 2026</text></svg><svg viewBox='0 0 800 200' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><text x='400' y='20' text-anchor='middle' fill='#E95420' font-size='15' font-weight='bold'>Packfile Structure: Delta Compression</text><rect x='20' y='40' width='130' height='60' rx='6' fill='#C34113'/><text x='85' y='65' text-anchor='middle' fill='#fff' font-size='10' font-weight='bold'>Commit A (base)</text><text x='85' y='82' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>SHA: a1b2c3</text><path d='M150 70 L190 70' stroke='#C34113' stroke-width='2' marker-end='url(#an-dn)'/><rect x='200' y='40' width='130' height='60' rx='6' fill='#E95420'/><text x='265' y='65' text-anchor='middle' fill='#fff' font-size='10' font-weight='bold'>Delta: Commit B</text><text x='265' y='82' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>diff from A (~5%)</text><path d='M330 70 L370 70' stroke='#E95420' stroke-width='2' marker-end='url(#an-dn)'/><rect x='380' y='40' width='130' height='60' rx='6' fill='#238636'/><text x='445' y='65' text-anchor='middle' fill='#fff' font-size='10' font-weight='bold'>Delta: Commit C</text><text x='445' y='82' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>diff from B (~2%)</text><path d='M510 70 L550 70' stroke='#238636' stroke-width='2' marker-end='url(#an-dn)'/><rect x='560' y='40' width='130' height='60' rx='6' fill='#F0883E'/><text x='625' y='65' text-anchor='middle' fill='#fff' font-size='10' font-weight='bold'>Delta: Commit D</text><text x='625' y='82' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>diff from C (~1%)</text><text x='400' y='130' text-anchor='middle' fill='var(--text-secondary)' font-size='10'>Packfiles store base object + delta chain → efficient storage</text><rect x='250' y='145' width='300' height='35' rx='6' fill='var(--bg-secondary)' stroke='#E95420' stroke-width='1.5'/><text x='400' y='167' text-anchor='middle' fill='#E95420' font-size='10'>Object types: blob (file), tree (dir), commit (snapshot), tag (annotation)</text><rect x=\"0\" y=\"186\" width=\"800\" height=\"14\" fill=\"rgba(0,0,0,0.3)\" rx=\"0\" /><text x=\"796\" y=\"196\" text-anchor=\"end\" fill=\"rgba(255,255,255,0.5)\" font-size=\"8\" font-family=\"monospace\">git-bytes  © 2026</text><rect x=\"0\" y=\"186\" width=\"800\" height=\"14\" fill=\"rgba(0,0,0,0.3)\" rx=\"0\" /><text x=\"796\" y=\"196\" text-anchor=\"end\" fill=\"rgba(255,255,255,0.5)\" font-size=\"8\" font-family=\"monospace\">git-bytes  © 2026</text></svg></p>",
  "sections": [
    {
      "title": "The Object Store",
      "description": "<p>All Git objects live in <code>.git/objects/</code>, stored by SHA-1 hash (first 2 chars = directory, remaining 38 = filename). Objects are zlib-compressed. Use <code>git cat-file -p</code> to inspect any object by hash. Every object has a type header: <code>blob</code>, <code>tree</code>, <code>commit</code>, or <code>tag</code>.</p><pre><code>git cat-file -p HEAD          # inspect commit\ngit hash-object file.txt      # compute SHA-1\ngit ls-tree HEAD              # list tree entries</code></pre>"
    },
    {
      "title": "Blobs & Trees",
      "description": "<p>A <strong>blob</strong> stores file content — just the bytes, no name or metadata. A <strong>tree</strong> represents a directory: it maps filenames to blob SHAs, with mode (100644 = file, 100755 = executable, 040000 = subdirectory). Trees can reference other trees (subdirectories).</p><pre><code>git ls-tree -r HEAD           # recursive tree\ngit ls-tree -l HEAD           # with object sizes\ngit write-tree                # write tree from index</code></pre>"
    },
    {
      "title": "Commits & Tags",
      "description": "<p>A <strong>commit</strong> object contains: tree SHA (root directory), parent SHA(s), author/committer (name + email + timestamp), and message. An <strong>annotated tag</strong> points to a commit and adds a tagger, message, and optional GPG signature. Lightweight tags are just refs.</p><pre><code>git cat-file commit HEAD      # raw commit content\ngit cat-file tag v1.0         # raw tag content\ngit show HEAD --format=raw    # full commit details</code></pre>"
    },
    {
      "title": "References & HEAD",
      "description": "<p>Refs are pointers stored in <code>.git/refs/</code>: <code>heads/</code> (local branches), <code>remotes/</code> (remote branches), <code>tags/</code>. <code>HEAD</code> is a symbolic ref pointing to the current branch (e.g., <code>ref: refs/heads/main</code>). Detached HEAD means HEAD points directly to a commit.</p><pre><code>git symbolic-ref HEAD         # show HEAD target\ngit rev-parse HEAD            # resolve to SHA\ngit rev-parse --symbolic-full-name HEAD</code></pre>"
    },
    {
      "title": "Packfiles & GC",
      "description": "<p>Git auto-packs loose objects into <strong>packfiles</strong> (<code>.idx</code> + <code>.pack</code>) for efficiency. Packfiles use <strong>delta compression</strong>: similar objects are stored as base + delta chain. <code>git gc</code> runs manually or automatically. It prunes unreachable objects (older than 2 weeks by default) and repacks remaining objects.</p><pre><code>git gc --aggressive          # heavy optimization\ngit count-objects -v         # object statistics\ngit verify-pack .git/objects/pack/*.idx</code></pre>"
    }
  ],
  "details": "<p>Git objects are immutable — once created, they never change. The SHA-1 hash is computed as <code>sha1(type + ' ' + size + '\\0' + content)</code>. This means identical files (same bytes) always produce the same blob SHA, regardless of filename or location — Git automatically deduplicates. The object graph is a Merkle DAG: the commit hash depends on the tree hash, which depends on the blob hashes. This cryptographic integrity means any change to any file at any point in history is detectable. Packfiles can be thin (used during fetch/push) or self-contained. Git's GC uses a generational approach: recent objects are loose; older objects get packed. The <code>--window</code> and <code>--depth</code> parameters control delta search effort.</p>",
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>git cat-file -p HEAD</code> — inspect any object (commit, tree, blob) by SHA or ref.</li><li><code>git ls-tree -r HEAD</code> — recursive tree listing: all files with modes and SHAs.</li><li><code>git hash-object file.txt</code> — compute the SHA-1 hash a file would have as a blob.</li><li><code>git count-objects -v</code> — object count, packfile info, and GC statistics.</li></ul><h4>Staff+ Patterns</h4><ul><li>Use <code>git cat-file --batch</code> for efficient bulk object inspection in scripts — avoids one process per object.</li><li>Understand that identical files deduplicate automatically. This makes Git efficient for monorepos with many similar files (templates, configs).</li></ul><h4>Watch Outs</h4><ul><li><code>git gc --aggressive</code> can take hours on large repos and provides marginal benefit. Prefer <code>git gc --auto</code>.</li><li>Loose objects older than 2 weeks are pruned by <code>git gc</code>. If you need an object, ensure it's reachable from a ref or reflog.</li></ul><h4>Interview Angle</h4><p>Google L6 asks about Git hosting design — knowing the object model is table stakes. Explain the Merkle DAG property: because commit hashes depend on tree hashes which depend on blob hashes, any corruption anywhere is detectable. This is why Git is considered a content-addressable, integrity-verified filesystem.</p>",
  "tags": [
    "git",
    "internals",
    "plumbing"
  ],
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}