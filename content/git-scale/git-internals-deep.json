{
  "id": "git-internals-deep",
  "title": "Git Internals Deep Dive",
  "category": "Git at Scale",
  "subcategory": "Internals",
  "language": "bash",
  "description": "<p>Explore Git's internal data structures — commit-graph, reachability bitmaps, and MIDX files — that power efficient operations at scale, and learn the same debugging techniques used by Git core contributors.</p><svg viewBox='0 0 800 260' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><defs><marker id='gi-blue' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#C34113'/></marker><marker id='gi-bright' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#E95420'/></marker></defs><rect x='10' y='10' width='780' height='230' rx='8' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='1'/><text x='400' y='36' text-anchor='middle' fill='#fff' font-size='14' font-weight='bold'>Commit-Graph Reachability Explorer</text><line x1='30' y1='46' x2='770' y2='46' stroke='var(--border-default)' stroke-width='1'/><text x='400' y='66' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>How Git finds merge bases efficiently using commit-graph</text><circle cx='120' cy='110' r='20' fill='#C34113'/><text x='120' y='115' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>C1</text><circle cx='220' cy='90' r='20' fill='#E95420'/><text x='220' y='95' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>C2</text><circle cx='320' cy='70' r='20' fill='#C34113'/><text x='320' y='75' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>C3</text><circle cx='420' cy='120' r='20' fill='#E95420'/><text x='420' y='125' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>C4</text><circle cx='520' cy='100' r='20' fill='#C34113'/><text x='520' y='105' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>C5</text><circle cx='620' cy='140' r='20' fill='#E95420'/><text x='620' y='145' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>C6</text><circle cx='700' cy='110' r='20' fill='#C34113'/><text x='700' y='115' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>C7</text><path d='M140 110 L200 90' stroke='#C34113' stroke-width='2' marker-end='url(#gi-blue)'/><path d='M240 90 L300 70' stroke='#E95420' stroke-width='2' marker-end='url(#gi-bright)'/><path d='M340 70 L400 118' stroke='#C34113' stroke-width='2' marker-end='url(#gi-blue)'/><path d='M320 90 L500 96' stroke='#E95420' stroke-width='2' marker-end='url(#gi-bright)' stroke-dasharray='4 2'/><path d='M440 120 L500 104' stroke='#C34113' stroke-width='2' marker-end='url(#gi-blue)' stroke-dasharray='4 2'/><path d='M520 120 L600 136' stroke='#E95420' stroke-width='2' marker-end='url(#gi-bright)'/><path d='M540 100 L680 106' stroke='#C34113' stroke-width='2' marker-end='url(#gi-blue)'/><path d='M640 140 L680 114' stroke='#E95420' stroke-width='2' marker-end='url(#gi-bright)'/><text x='400' y='180' text-anchor='middle' fill='rgba(136,145,164,0.9)' font-size='11'>Commit-graph stores generation numbers (GENERATION_NUMBER_MAX for each commit)</text><text x='400' y='200' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>Merge base of C6 and C7: walk backwards using generation numbers — skip non-candidates</text><text x='400' y='225' text-anchor='middle' fill='rgba(136,145,164,0.8)' font-size='11'>Without commit-graph: O(N) walk. With commit-graph: walks only commits in intersection, skipping ahead by generation</text></svg><svg viewBox='0 0 800 220' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><rect x='10' y='10' width='780' height='190' rx='8' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='1'/><text x='400' y='36' text-anchor='middle' fill='#fff' font-size='14' font-weight='bold'>Bitmap Index Structure for Efficient Clone/Fetch</text><line x1='30' y1='46' x2='770' y2='46' stroke='var(--border-default)' stroke-width='1'/><rect x='80' y='60' width='180' height='36' rx='4' fill='#C34113'/><text x='170' y='83' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Packfile (.pack)</text><rect x='540' y='60' width='180' height='36' rx='4' fill='#E95420'/><text x='630' y='83' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Bitmap (.bitmap)</text><line x1='260' y1='78' x2='530' y2='78' stroke='#E95420' stroke-width='2' stroke-dasharray='5 3'/><text x='400' y='78' text-anchor='middle' fill='rgba(136,145,164,0.7)' font-size='10'>index by object</text><rect x='80' y='110' width='200' height='70' rx='4' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='1'/><text x='180' y='132' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='11'>Packfile objects</text><text x='180' y='150' text-anchor='middle' fill='rgba(255,255,255,0.6)' font-size='10'>Commit 1, Tree A, Blob X</text><text x='180' y='166' text-anchor='middle' fill='rgba(255,255,255,0.6)' font-size='10'>Commit 2, Tree B, Blob Y</text><rect x='520' y='110' width='220' height='70' rx='4' fill='var(--bg-secondary)' stroke='#E95420' stroke-width='1'/><text x='630' y='132' text-anchor='middle' fill='#E95420' font-size='11'>Bitmap: [1 1 0 1 0 0 1 1 ...]</text><text x='630' y='150' text-anchor='middle' fill='rgba(255,255,255,0.6)' font-size='10'>Bit position = object index in pack</text><text x='630' y='166' text-anchor='middle' fill='rgba(255,255,255,0.6)' font-size='10'>EOR walk to compute reachability</text><text x='400' y='210' text-anchor='middle' fill='rgba(136,145,164,0.8)' font-size='11'>Bitmaps enable O(1) reachability checks — essential for large clone/fetch, replaces O(N) object enumeration</text></svg>",
  "sections": [
    {
      "title": "Commit-Graph",
      "description": "<p>The commit-graph file (<code>.git/objects/info/commit-graph</code>) stores commit metadata — parent IDs, generation numbers, commit timestamps — in a compressed binary format. It speeds up <code>git log --graph</code>, merge-base computation, and reachability queries by 10-100x. Enable via <code>git commit-graph write</code>. Git 2.18+ writes it automatically during <code>git gc</code>.</p>",
      "codeBlock": "# Write commit-graph for reachability speedup\ngit commit-graph write --reachable\n# Writes .git/objects/info/commit-graph\n\n# Write commit-graph for all refs\ngit commit-graph write --stdin-commits < <(git rev-list --all)\n\n# Verify commit-graph integrity\ngit commit-graph verify\n\n# Check commit-graph stats\ngit commit-graph read --show-format\n\n# Enable commit-graph auto-writes\ngit config core.commitGraph true\ngit config gc.writeCommitGraph true\n\n# Performance comparison\ntime git merge-base feature main\n# Without commit-graph: ~12s for 500K commits\n# With commit-graph: ~0.08s"
    },
    {
      "title": "Reachability Bitmaps",
      "description": "<p>Bitmaps (<code>.bitmap</code> files paired with packfiles) track which objects are reachable from each ref using a bit array. One bit per object per ref. Git uses EWAH (Enhanced Word-Aligned Hybrid) compression for sparse bitmaps. Clone/fetch performance: instead of walking the entire DAG to enumerate objects, Git ORs bitmaps for the requested refs — O(1) object enumeration.</p>",
      "codeBlock": "# Bitmap index files (auto-generated during repack)\ngit repack -adb  # creates bitmap index\n# Generates: .git/objects/pack/pack-<hash>.bitmap\n\n# Check if bitmap exists\nls .git/objects/pack/*.bitmap\n\n# Inspect pack with bitmap\ngit verify-pack -v .git/objects/pack/pack-*.idx | head -20\n\n# Count bitmap-reachable objects\ngit rev-list --count --objects --all --use-bitmap-index\n# vs without:\ngit rev-list --count --objects --all  # slower, no bitmap\n\n# Force bitmap creation\ngit config pack.writeBitmapHashCache true\ngit config pack.writeReverseIndex true"
    },
    {
      "title": "MIDX Files",
      "description": "<p>Multi-pack index (MIDX) files enable Git to query across multiple packfiles efficiently. Without MIDX, Git opens each idx file sequentially. MIDX creates a unified index (<code>.git/objects/pack/multi-pack-index</code>) that maps object IDs to packfile + offset. Essential for repositories with many small packfiles (frequent fetches, incremental repacks).</p>",
      "codeBlock": "# Create multi-pack index\ngit multi-pack-index write\n\n# Verify MIDX integrity\ngit multi-pack-index verify\n\n# Expire old packfiles (remove unreachable objects)\ngit multi-pack-index expire\n\n# Repack using MIDX (consolidate packfiles)\ngit multi-pack-index repack --batch-size=2G\n\n# MIDX + bitmap integration\ngit repack --write-midx --write-bitmap-index -d\n\n# Check MIDX stats\ngit multi-pack-index --object-dir=.git/objects verify 2>&1\n\n# Performance: query across packfiles\ngit cat-file --batch-check --batch-all-objects\n# With MIDX: 2x faster than scanning all idx files"
    },
    {
      "title": "Performance Tuning",
      "description": "<p>Key Git configs for large repositories: <code>core.preloadIndex=true</code> (parallel index loading), <code>core.fsmonitor</code> (watchman for file system changes), <code>core.untrackedCache=true</code> (cache untracked file scan), <code>core.commitGraph=true</code>, <code>feature.manyFiles=true</code> (enables all large-repo optimizations), <code>protocol.version=2</code> (faster negotiation). Use Scalar for Microsoft's enhanced Git client.</p>",
      "codeBlock": "# Performance tuning for large repos\ngit config feature.manyFiles true\n# Enables: commitGraph, fsmonitor, indexVersion >= 4, untrackedCache\n\n# Enable protocol v2 for faster fetch negotiation\ngit config protocol.version 2\n\n# Preload index for faster status\ngit config core.preloadIndex true\n\n# Use file system monitor (watchman required)\ngit config core.fsmonitor true\n# Falls back to: git config core.fsmonitor none\n\n# Set delta cache size\ngit config core.deltaBaseCacheLimit 2g\n\n# Enable sparse index\ngit sparse-checkout init --cone --sparse-index\n\n# Install Scalar (Microsoft)\n# brew install microsoft/git/scalar\nscalar register\nscalar clone <url> --single-branch"
    },
    {
      "title": "Debugging Tools",
      "description": "<p>Git's internal debugging tools: <code>GIT_TRACE=1</code> (trace git commands), <code>GIT_TRACE_PACKET=1</code> (trace network protocol), <code>GIT_TRACE_PERFORMANCE=1</code> (timing data), <code>GIT_TRACE_SETUP=1</code> (path resolution). Use <code>git rev-list --debug</code> for walk details, <code>git pack-objects --debug</code> for packing decisions, and <code>git fsck --strict</code> for repo health.</p>",
      "codeBlock": "# Trace environment variables\nexport GIT_TRACE=1\nexport GIT_TRACE_PERFORMANCE=1\nexport GIT_TRACE_PACKET=1\nexport GIT_TRACE_SETUP=1\n\n# Profile specific commands\nGIT_TRACE_PERFORMANCE=2 git status\n\n# Debug object lookup\ngit rev-list --debug --all --max-count=10\n\n# Debug pack file contents\ngit verify-pack -v .git/objects/pack/pack-*.idx | grep -E \"commit|blob|tree\"\n\n# Debug reachability\ngit rev-list --objects --all --use-bitmap-index --count\n\n# Debug protocol negotiation\ngit -c protocol.version=2 fetch --verbose origin\n\n# Analyze repository\ngit filter-repo --analyze\n\n# Count objects by type\ngit cat-file --batch-check --batch-all-objects | awk '{print $2}' | sort | uniq -c | sort -rn"
    }
  ],
  "details": "<p>Understanding Git internals is essential for Staff+ engineers debugging performance issues or customizing Git for organizational needs. The commit-graph binary format stores: OID, parent offsets, root tree OID, generation number, commit timestamp. Generation numbers are the key innovation — they enable topological sorting without graph traversal and allow pruning during merge-base computation. Bitmaps use a two-level structure: a commit bitmap (one per ref) and a type bitmap (commits/trees/blobs/tags). MIDX files solve the 'many packfiles' problem — repos with 100+ packfiles see 5-10x query slowdown without MIDX. Microsoft's Scalar adds prefetch, background maintenance, and GVFS protocol for Windows. For deep debugging, build Git from source with <code>DEVELOPER=1</code> for extra assertions and <code>gdb</code> breakpoints.</p>",
  "tags": [
    "staff+",
    "git",
    "internals",
    "performance"
  ],
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>git cat-file --batch</code> — bulk object inspection. Pipe object SHAs for efficient scanning.</li><li><code>git verify-pack .git/objects/pack/*.idx</code> — validate packfile integrity.</li><li><code>git rev-list --objects --all</code> — list all objects in the repo.</li><li><code>git gc --auto</code> — auto-garbage-collect. Prefer over <code>--aggressive</code>.</li></ul><h4>Staff+ Patterns</h4><ul><li>Use <code>git cat-file --batch-check</code> in scripts to identify large objects: pipe <code>git rev-list --objects --all | awk '{print $1}'</code> to batch-check with object sizes.</li><li>Monitor <code>git count-objects -v</code> for loose object count. High loose count = GC needed. Automate GC in cron.</li></ul><h4>Watch Outs</h4><ul><li><code>git gc --aggressive</code> can take hours on large repos and provides marginal benefit over <code>git gc --auto</code>.</li><li>Delta compression in packfiles is a read-time trade-off: deeper delta chains = smaller packs but slower object access.</li></ul><h4>Interview Angle</h4><p>Google L6 asks about Git hosting design. Understanding the object model and Merkle DAG is essential. Know that the SHA-1 hash is computed as <code>sha1(type + ' ' + size + '\\0' + content)</code> and that identical files always produce the same blob hash (automatic dedup).</p>"
}