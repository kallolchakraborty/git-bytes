{
  "id": "monorepo-management",
  "title": "Monorepo Management",
  "category": "Git at Scale",
  "subcategory": "Monorepo",
  "language": "bash",
  "description": "<p>Scale Git to handle thousands of developers and millions of files using partial clone, sparse checkout, shallow clones, and the same techniques powering Google, Meta, and Microsoft monorepos.</p><svg viewBox=\"0 0 550 160\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%;height:auto;display:block;margin:1.5rem 0\" font-family=\"Ubuntu,sans-serif\"><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0s\" fill=\"freeze\"/><text x=\"137\" y=\"20\" text-anchor=\"middle\" fill=\"#E95420\" font-size=\"14\" font-weight=\"700\">Full Clone</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.8s\" fill=\"freeze\"/><text x=\"412\" y=\"20\" text-anchor=\"middle\" fill=\"#E95420\" font-size=\"14\" font-weight=\"700\">Partial Clone</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.2s\" fill=\"freeze\"/><rect x=\"40\" y=\"35\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#C34113\"/><text x=\"87\" y=\"53\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"10\" font-weight=\"700\">Commits</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.5s\" fill=\"freeze\"/><rect x=\"40\" y=\"70\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#E95420\"/><text x=\"87\" y=\"88\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"10\" font-weight=\"700\">Trees</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.8s\" fill=\"freeze\"/><rect x=\"40\" y=\"105\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#DA3633\"/><text x=\"87\" y=\"123\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"10\" font-weight=\"700\">Blobs</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.2s\" fill=\"freeze\"/><rect x=\"315\" y=\"35\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#C34113\"/><text x=\"362\" y=\"53\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"10\" font-weight=\"700\">Commits</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.5s\" fill=\"freeze\"/><rect x=\"315\" y=\"70\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#E95420\"/><text x=\"362\" y=\"88\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"10\" font-weight=\"700\">Trees</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.8s\" fill=\"freeze\"/><rect x=\"315\" y=\"105\" width=\"60\" height=\"28\" rx=\"6\" fill=\"#238636\"/><text x=\"345\" y=\"123\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"10\" font-weight=\"700\">Lazy</text></g></svg>",
  "sections": [
    {
      "title": "Partial Clone",
      "description": "<p>Partial clone defers blob downloads until needed. Use <code>--filter=blob:none</code> to fetch commits and trees only — blobs download on checkout. <code>--filter=tree:0</code> skips trees too. <code>--filter=blob:limit=1M</code> fetches small blobs immediately, defers large ones. Pair with <code>git sparse-checkout</code> for maximum efficiency. Promisor remotes fetch missing objects on demand.</p>",
      "codeBlock": "# Partial clone with blob:none (recommended)\ngit clone --filter=blob:none <url>\n# Initial: ~150MB instead of ~5GB for large monorepos\n\n# Partial clone with tree:0 (deepest partial)\ngit clone --filter=tree:0 <url>\n# Only commit objects initially — trees + blobs on demand\n\n# Blob size limit filter\ngit clone --filter=blob:limit=1M <url>\n# Blobs >1MB are deferred\n\n# Configure promisor remote (optional)\ngit config remote.origin.promisor true\ngit config remote.origin.partialclonefilter blob:none"
    },
    {
      "title": "Sparse Checkout",
      "description": "<p>Sparse checkout limits which files appear in your working tree. Cone mode (<code>git sparse-checkout set --cone</code>) is the modern approach: specify top-level directories, and Git only populates those. Parents are included implicitly. This dramatically reduces <code>git status</code> time and disk usage. Combine with partial clone for optimal monorepo performance.</p>",
      "codeBlock": "# Enable sparse checkout (modern cone mode)\ngit sparse-checkout init --cone\n\n# Set directories to work on\ngit sparse-checkout set src/frontend src/shared\n\n# Add additional directories\ngit sparse-checkout add docs\n\n# Check current sparse checkout state\ngit sparse-checkout list\n\n# Disable sparse checkout (checkout everything)\ngit sparse-checkout disable\n\n# Legacy non-cone mode (pattern-based)\ngit sparse-checkout init  # without --cone\necho \"src/frontend/*\" >> .git/info/sparse-checkout"
    },
    {
      "title": "Shallow Clones",
      "description": "<p>Shallow clones truncate history with <code>--depth=N</code>. Useful for CI (only need latest commit) and exploratory cloning. <code>--depth=1</code> fetches one commit — fastest possible clone. <code>--shallow-since=<date></code> fetches commits since a date. <code>--shallow-exclude=<ref></code> excludes commits reachable from a ref. Unshallow later with <code>git fetch --unshallow</code>.</p>",
      "codeBlock": "# Shallow clone (single commit — fastest)\ngit clone --depth=1 <url>\n\n# Shallow clone with history since a date\ngit clone --shallow-since=\"2024-01-01\" <url>\n\n# Shallow clone excluding specific refs\ngit clone --shallow-exclude=v1.0 <url>\n\n# Convert shallow to full clone\ngit fetch --unshallow\n\n# Deepen shallow clone by N commits\ngit fetch --depth=100\n\n# CI optimization: shallow clone + fetch PR merge commit\n# GitHub Actions\ngit fetch --depth=1 origin ${{ github.event.pull_request.merge_commit_sha }}"
    },
    {
      "title": "filter-repo Migration",
      "description": "<p>When migrating from or to monorepo, <code>git filter-repo</code> is the modern replacement for <code>git filter-branch</code> and <code>BFG Repo-Cleaner</code>. It's 10-100x faster, handles edge cases correctly, and natively supports Python for custom callbacks. Use for splitting monorepos into sub-repos, combining repos, or removing history branches.</p>",
      "codeBlock": "# Install git-filter-repo\npip install git-filter-repo\n# or: brew install git-filter-repo\n\n# Split a monorepo — extract a subdirectory into its own repo\ngit filter-repo --path src/frontend/ --path src/shared/\n\n# Combine multiple repos into a monorepo\ngit filter-repo --path-rename old-repo/:new-repo/old-repo/\n\n# Remove all but a specific set of paths\ngit filter-repo --path-glob '*.py' --path-glob '*.js' --invert-paths\n\n# Remove large files from history\ngit filter-repo --strip-blobs-bigger-than 10M\n\n# Analyze repository history\ngit filter-repo --analyze\n# Results in .git/filter-repo/analysis/"
    },
    {
      "title": "CI/CD for Monorepos",
      "description": "<p>Monorepo CI challenges: only build what changed. Use <code>git diff</code> to detect changed paths, dependency graph analysis (Bazel, Nx, Turborepo) to determine affected projects. GitHub Actions offers path filters. For large monorepos, implement build graph caching and distributed builds. Use merge queues to batch compatible changes.</p>",
      "codeBlock": "# GitHub Actions: Path-based triggers\non:\n  pull_request:\n    paths:\n      - 'src/frontend/**'\n      - 'src/shared/**'\n\n# Detect changed projects in monorepo\nCHANGED=$(git diff --name-only HEAD~1)\nif echo \"$CHANGED\" | grep -q \"^src/frontend/\"; then\n  cd src/frontend && npm run build\nfi\n\n# Bazel query for affected targets\nbazel query 'kind(\".*_binary\", rdeps(//..., set($(git diff --name-only HEAD~1))))'\n\n# Nx affected command\nnpx nx affected:test --base=main --head=HEAD\n\n# Monorepo CI flow\n# 1. Filter: identify changed projects\n# 2. Build: only changed + dependents\n# 3. Test: run affected test suites\n# 4. Lint: scope to changed files\n# 5. Package: publish only changed packages"
    }
  ],
  "details": "<p>Monorepo management at FAANG scale relies on three pillars: <strong>client-side filtering</strong> (partial clone + sparse checkout), <strong>server-side optimization</strong> (reachability bitmaps, MIDX, commit-graph), and <strong>build system integration</strong> (Bazel, Buck, Please). Google's Piper monorepo (2B+ lines, 25K+ developers) uses a proprietary VCS but Git monorepos can approach similar scale with Scalar (Microsoft's Git extension). Key benchmarks: partial clone reduces initial clone from 4.5 GB to 145 MB (97% reduction); sparse checkout reduces <code>git status</code> from 45s to 0.3s; commit-graph speeds up merge-base from 12s to 0.8s. The critical insight: Git is not slow for monorepos — it's slow when fetching/checking things you don't need.</p>",
  "tags": [
    "staff+",
    "git",
    "monorepo",
    "scale"
  ],
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>git clone --filter=blob:none &lt;url&gt;</code> — partial clone: fetch commits/trees but defer blobs.</li><li><code>git sparse-checkout set --cone src/</code> — limit working tree to specific directories.</li><li><code>git maintenance start</code> — background maintenance (fetch, commit-graph, loose-objects).</li></ul><h4>Staff+ Patterns</h4><ul><li>Combine partial clone + sparse checkout for monorepo performance: <code>--filter=tree:0</code> defers ALL object fetching, <code>--cone mode</code> limits to your team's directory.</li><li>Use <code>git fsmonitor--daemon</code> with <code>watchman</code> to eliminate <code>lstat()</code> calls — makes <code>git status</code> instant even in large working trees.</li></ul><h4>Watch Outs</h4><ul><li>Partial clone requires Git 2.26+ and server-side support (GitHub supports it since 2021). Older Git versions fall back to full clone.</li><li>Blobless clones (<code>--filter=blob:none</code>) still fetch blobs on demand — the first <code>git log -p</code> will be slow as blobs are lazily fetched.</li></ul><h4>Interview Angle</h4><p>Meta L6–L7 asks about monorepo architecture. Key insight: 90% of engineers only touch 10% of the codebase. Partial clone + sparse checkout + fsmonitor make monorepos practical at 10K+ engineer scale. CI must use dependency graph analysis to build only affected projects.</p>",
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}