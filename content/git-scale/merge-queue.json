{
  "id": "merge-queue",
  "title": "Advanced Merge Strategies",
  "category": "Git at Scale",
  "subcategory": "Merge",
  "language": "bash",
  "description": "<p>Master GitHub Merge Queue architecture, semantic conflict detection, and the merge algorithms used by FAANG engineering teams to maintain trunk stability at scale.</p><svg viewBox='0 0 800 280' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><defs><marker id='mq-blue' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#C34113'/></marker><marker id='mq-bright' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#E95420'/></marker><marker id='mq-green' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#3FB950'/></marker></defs><rect x='10' y='10' width='780' height='250' rx='8' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='1'/><text x='400' y='36' text-anchor='middle' fill='#fff' font-size='14' font-weight='bold'>GitHub Merge Queue Orchestrator</text><line x1='30' y1='46' x2='770' y2='46' stroke='var(--border-default)' stroke-width='1'/><text x='400' y='65' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>Queued PRs → Batch Build → Merge → Next Batch</text><rect x='30' y='80' width='80' height='60' rx='6' fill='#C34113'/><text x='70' y='105' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>PR #1</text><text x='70' y='124' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>feat/a</text><path d='M110 110 L140 110' stroke='#E95420' stroke-width='2' marker-end='url(#mq-bright)'/><rect x='150' y='80' width='80' height='60' rx='6' fill='#C34113'/><text x='190' y='105' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>PR #2</text><text x='190' y='124' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>fix/b</text><path d='M230 110 L260 110' stroke='#E95420' stroke-width='2' marker-end='url(#mq-bright)'/><rect x='270' y='80' width='80' height='60' rx='6' fill='#C34113'/><text x='310' y='105' text-anchor='middle' fill='#fff' font-size='11' font-weight='bold'>PR #3</text><text x='310' y='124' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='9'>chore/c</text><path d='M350 110 L380 110' stroke='#E95420' stroke-width='2' marker-end='url(#mq-bright)'/><rect x='390' y='80' width='120' height='60' rx='6' fill='var(--bg-secondary)' stroke='#E95420' stroke-width='2'/><text x='450' y='105' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>Batch Build</text><text x='450' y='124' text-anchor='middle' fill='rgba(233,84,32,0.7)' font-size='9'>Integration CI</text><path d='M510 110 L540 110' stroke='#E95420' stroke-width='2' marker-end='url(#mq-bright)'/><rect x='550' y='80' width='80' height='60' rx='6' fill='var(--bg-secondary)' stroke='#3FB950' stroke-width='2'/><text x='590' y='105' text-anchor='middle' fill='#3FB950' font-size='12' font-weight='bold'>Merge</text><text x='590' y='124' text-anchor='middle' fill='rgba(63,185,80,0.7)' font-size='9'>to main</text><path d='M630 110 L660 110' stroke='#3FB950' stroke-width='2' marker-end='url(#mq-green)'/><rect x='670' y='80' width='100' height='60' rx='6' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='2'/><text x='720' y='105' text-anchor='middle' fill='rgba(255,255,255,0.5)' font-size='11'>Next</text><text x='720' y='124' text-anchor='middle' fill='rgba(255,255,255,0.5)' font-size='9'>Batch</text><text x='400' y='175' text-anchor='middle' fill='rgba(136,145,164,0.8)' font-size='11'>Batch detection: Git walks commit graph to identify non-conflicting PRs — builds them together</text><text x='400' y='195' text-anchor='middle' fill='rgba(136,145,164,0.8)' font-size='11'>On failure: queue drops failing PR, retries remaining batch without it</text><rect x='160' y='210' width='140' height='28' rx='4' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='1'/><text x='230' y='229' text-anchor='middle' fill='rgba(255,255,255,0.6)' font-size='10'>Batch 1: PR#1 + PR#3 ✓</text><rect x='500' y='210' width='140' height='28' rx='4' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='1'/><text x='570' y='229' text-anchor='middle' fill='rgba(255,255,255,0.6)' font-size='10'>Batch 2: PR#2 + PR#4 ✓</text></svg><svg viewBox='0 0 800 200' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><rect x='10' y='10' width='780' height='170' rx='8' fill='var(--bg-secondary)' stroke='var(--border-default)' stroke-width='1'/><text x='400' y='36' text-anchor='middle' fill='#fff' font-size='14' font-weight='bold'>Merge Algorithm Comparison</text><line x1='30' y1='46' x2='770' y2='46' stroke='var(--border-default)' stroke-width='1'/><rect x='30' y='60' width='220' height='46' rx='6' fill='#C34113'/><text x='140' y='76' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>3-Way Merge</text><text x='140' y='94' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='10'>base + ours + theirs → diff3</text><rect x='290' y='60' width='220' height='46' rx='6' fill='#E95420'/><text x='400' y='76' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Recursive Merge</text><text x='400' y='94' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='10'>merge base → virtual base → merge</text><rect x='550' y='60' width='220' height='46' rx='6' fill='var(--bg-secondary)' stroke='#E95420' stroke-width='2'/><text x='660' y='76' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>Octopus Merge</text><text x='660' y='94' text-anchor='middle' fill='rgba(233,84,32,0.8)' font-size='10'>3+ branches simultaneously</text><text x='400' y='130' text-anchor='middle' fill='rgba(136,145,164,0.8)' font-size='11'>3-Way: Simple, fast, default. Recursive: Handles criss-cross merges. Octopus: N-way for topic branches.</text><text x='400' y='155' text-anchor='middle' fill='rgba(136,145,164,0.8)' font-size='11'>Git's default is recursive with the 'patience' or 'histogram' diff algorithm for better diff quality</text></svg>",
  "sections": [
    {
      "title": "Merge Queue Architecture",
      "description": "<p>GitHub Merge Queue (GA since 2023) maintains trunk stability by batching PRs, running CI on the combined result, and merging only when green. Architecture: <strong>Queuing</strong> — PRs enter queue order or priority. <strong>Batching</strong> — system detects non-conflicting PRs via graph analysis. <strong>Integration build</strong> — CI runs on squash-merged integration branch. <strong>Merge</strong> — if green, commits are fast-forwarded to main. <strong>Failure recovery</strong> — queue drops failing PR, rebuilds remainder.</p>",
      "codeBlock": "# GitHub Merge Queue configuration\n# .github/merge-queue.yml\ngroup:\n  pattern: \"batch-*\"\n  batch_size: 5\n  batch_timeout: 3600  # 1 hour\n\ncheck_rules:\n  - name: \"build\"\n    type: \"checks\"\n    skip_broken: false\n  - name: \"test\"\n    type: \"checks\"\n  - name: \"code review\"\n    type: \"approvals\"\n    min_approvals: 1\n\n# Using Merge Queue via GH CLI\ngh pr merge --auto --subject \"feat: add OAuth\" --body \"Closes #123\"\n\n# Check queue status\ngh pr view --json mergeQueue\n\n# Manual queue interaction\ngit checkout main\ngit merge-queue squash feature-branch"
    },
    {
      "title": "Semantic Conflicts",
      "description": "<p>Beyond textual merge conflicts: <strong>semantic conflicts</strong> occur when code compiles but behaves incorrectly — changed function signatures, renamed APIs, modified data schemas. Detection strategies: run full test suite on merge result, use static analysis (type checking), build graph analysis (Bazel <code>affected tests</code>), and integration testing. Google's Critique and Meta's Phabricator detect semantic conflicts by analyzing dependency graphs.</p>",
      "codeBlock": "# Semantic conflict detection techniques\n\n# 1. Run affected tests (Bazel)\nbazel test $(bazel query 'kind(\"test\", rdeps(//..., //src/changed:target))')\n\n# 2. TypeScript type checking on merge\nnpx tsc --noEmit --skipLibCheck\n\n# 3. Python mypy on changed files\ngit diff --name-only main...HEAD | xargs mypy\n\n# 4. GraphQL schema check (schema diff)\nnpx graphql-inspector diff schema-v1.gql schema-v2.gql\n\n# 5. Protobuf compatibility check\nbuf breaking --against .git#branch=main\n\n# Manual semantic conflict detection\n# Monorepo: run all tests that could be affected\ngit log --name-only HEAD..origin/main | sort -u | xargs -I{} dirname {} | sort -u | xargs npm test"
    },
    {
      "title": "Merge Algorithms",
      "description": "<p>Git supports multiple merge strategies: <code>recursive</code> (default) — finds merge base, creates virtual ancestor for criss-cross histories. <code>resolve</code> — simpler, faster for two-head merges. <code>octopus</code> — merges >2 heads at once for topic branch consolidation. <code>ours</code> — keeps our version completely. <code>subtree</code> — merges with prefix shifting. <code>ort</code> — the new recursive rewrite (Git 2.33+) using RFC 3 merge, faster and conflict-free rename handling.</p>",
      "codeBlock": "# Merge strategies\ngit merge feature              # recursive (default)\ngit merge -s resolve feature   # resolve (simpler, faster)\ngit merge -s octopus topic1 topic2 topic3  # octopus (N-way)\ngit merge -s ours feature      # keep our version\n\n# Merge strategy options\ngit merge -X theirs feature    # auto-resolve conflicts with theirs\ngit merge -X patience feature  # use patience diff algorithm\ngit merge -X diff-algorithm=histogram feature  # histogram diff\n\n# ORT strategy (Git 2.33+)\ngit merge -s ort feature       # new recursive implementation\n# Benefits: better rename detection, no index file needed, faster\n\n# Custom merge driver\necho \"*.cfg merge=custom-config\" >> .gitattributes\ngit config merge.custom-config.driver ./merge-config.sh"
    },
    {
      "title": "Automated Policies",
      "description": "<p>Enforce merge policies with GitHub branch protection and custom automation: <strong>Linear history</strong> — require rebase or squash merges. <strong>Signed commits</strong> — GPG or SSH signing required. <strong>Status checks</strong> — CI, lint, test, coverage, security scan. <strong>Up-to-date branches</strong> — require branch to be current with main. <strong>Review requirements</strong> — N approvals, code owner review. <strong>Auto-merge with conditions</strong> — merge when all checks pass.</p>",
      "codeBlock": "# Branch protection rules (GitHub API)\n# Require:\n# - Linear history (no merge commits)\n# - Required status checks (CI, lint, test)\n# - Up-to-date before merging\n# - Pull request reviews (1+ approval)\n# - Code owner review\n# - Signed commits\n\n# Auto-merge GitHub Action\nname: Auto Merge\non:\n  pull_request:\n    types: [labeled]\njobs:\n  auto-merge:\n    if: contains(github.event.pull_request.labels.*.name, 'auto-merge')\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: gh pr merge --auto --squash \"$PR_URL\"\n        env:\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n\n# Merge policy as code (graphql)\nmutation {\n  updateBranchProtectionRule(input: {\n    repositoryId: \"...\",\n    requiresLinearHistory: true,\n    requiresSignedCommits: true,\n    requiredApprovingReviewCount: 2\n  }) { clientMutationId }"
    },
    {
      "title": "FAANG Practices",
      "description": "<p><strong>Google</strong>: Piper's 'presubmit' pipeline — every change runs full test suite in a sandbox before landing. <strong>Meta</strong>: Phabricator + Sandcastle — diff-based CI with incremental builds; auto-reverts on test failure. <strong>Microsoft</strong>: VFS for Git + Scalar — virtual filesystem with on-demand file fetch; merge queue via Azure DevOps. <strong>Amazon</strong>: BRAZIL — monorepo with build dependency graph; automatic merge ordering by dependency.</p>",
      "codeBlock": "# Google's presubmit pipeline (simplified)\n# 1. Developer creates CL (changelist)\n# 2. Critique runs all affected tests in sandbox\n# 3. +2 code review + CI green\n# 4. Submitted to Piper trunk\n# 5. Piper auto-builds and may auto-revert on rollback\n\n# Meta's Sandcastle flow\ngit checkout -b my-diff\ngit commit -m \"my change\"\ngit push origin HEAD:refs/diff/my-diff\n# Sandcastle runs tests, Phabricator reviews\n# Landing: arc land (squash + rebase + push)\n\n# Microsoft's Scalar merge flow\nscalar clone <url>\ngit checkout -b feature\ngit commit -m \"change\"\ngit push origin feature\n# PR → Azure DevOps merge queue → CI → merge"
    }
  ],
  "details": "<p>At FAANG scale, merge strategies directly impact developer productivity and trunk stability. Google's Piper uses a custom VCS with transactional commits — there's no 'merge': changes are applied atomically to the tree. Meta's Mononoke (Git-compatible) supports pushrebase — rebase on push for linear history. GitHub Merge Queue implements a 'merge train' pattern: batches of PRs are tested together, and if any fail, the culprit is ejected and the remaining PRs retry. Semantic conflicts are the hardest to catch — they require type checking, static analysis, and full integration tests. The best merge strategy is the one your team understands and enforces consistently. For small teams (<10), squash-merge with linear history is ideal. For medium teams (10-100), add merge queues. For large orgs (100+), invest in custom tooling and dependency analysis.</p>",
  "tags": [
    "staff+",
    "git",
    "merge",
    "faang"
  ],
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>git merge --squash feature</code> — squash all feature commits into one.</li><li><code>git merge --no-ff feature</code> — force merge commit to preserve branch topology.</li><li><code>git merge-base --all main feature</code> — find the merge base(s) for criss-cross merges.</li><li><code>git merge-tree main feature</code> — preview merge result without actual merge (Git 2.34+).</li></ul><h4>Staff+ Patterns</h4><ul><li>Use a <strong>merge queue</strong> to prevent CI skew: PRs are batched and validated together before landing. GitHub's merge queue (since 2023) creates a temporary branch, runs CI on the combined diff, then fast-forwards main.</li><li>For the merge-ort strategy (Git 2.34+): it avoids index file locking issues in large repos and provides better conflict messages.</li></ul><h4>Watch Outs</h4><ul><li>Squash merge loses individual commit authorship. Use <code>--squash</code> only for feature branches, not for shared branches.</li><li>Merge queues add latency. Set a reasonable queue timeout and have a bypass for hotfixes.</li></ul><h4>Interview Angle</h4><p>Amazon L6 asks about merge algorithm internals. Know: recursive merge (default) vs ort (new), rename detection (50% similarity threshold), and the LCA computation for criss-cross histories. The merge queue is a distributed coordination problem.</p>",
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}