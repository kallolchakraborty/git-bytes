{
  "id": "gitops",
  "title": "GitOps & Deployments",
  "category": "Enterprise",
  "subcategory": "Deployments",
  "language": "bash",
  "description": "<p>GitOps uses Git as the single source of truth for declarative infrastructure and application deployments, with automated reconciliation loops.</p><svg viewBox='0 0 800 190' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><defs><marker id='ag-blue' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#C34113'/></marker><marker id='ag-bright' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#E95420'/></marker></defs><rect x='5' y='5' width='130' height='55' rx='8' fill='var(--bg-secondary)' stroke='#E95420' stroke-width='2'/><text x='70' y='28' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>Git Repo</text><text x='70' y='45' text-anchor='middle' fill='rgba(233,84,32,0.8)' font-size='10'>Desired state</text><path d='M135 32 L175 32' stroke='#E95420' stroke-width='2' marker-end='url(#ag-bright)'/><rect x='185' y='5' width='130' height='55' rx='8' fill='#C34113'/><text x='250' y='28' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Pull / Watch</text><text x='250' y='45' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='10'>Operator</text><path d='M315 32 L355 32' stroke='#C34113' stroke-width='2' marker-end='url(#ag-blue)'/><rect x='365' y='5' width='130' height='55' rx='8' fill='#E95420'/><text x='430' y='28' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Diff & Sync</text><text x='430' y='45' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='10'>Reconcile</text><path d='M495 32 L535 32' stroke='#E95420' stroke-width='2' marker-end='url(#ag-bright)'/><rect x='545' y='5' width='145' height='55' rx='8' fill='var(--bg-secondary)' stroke='#E95420' stroke-width='2'/><text x='617' y='28' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>Cloud Provider</text><text x='617' y='45' text-anchor='middle' fill='rgba(233,84,32,0.8)' font-size='10'>Actual state</text><path d='M617 60 L70 60 L70 75' stroke='#E95420' stroke-width='1.5' stroke-dasharray='4,3' marker-end='url(#ag-bright)'/><text x='370' y='90' text-anchor='middle' fill='#E95420' font-size='11' font-style='italic'>Reconciliation loop</text></svg><svg viewBox='0 0 800 160' xmlns='http://www.w3.org/2000/svg' style='max-width:100%;height:auto;display:block;margin:2rem 0'><defs><marker id='ag-blue2' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#C34113'/></marker><marker id='ag-bright2' markerWidth='10' markerHeight='7' refX='9' refY='3.5' orient='auto'><polygon points='0 0, 10 3.5, 0 7' fill='#E95420'/></marker></defs><rect x='5' y='10' width='130' height='55' rx='8' fill='#C34113'/><text x='70' y='33' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Actions</text><text x='70' y='50' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='10'>OIDC request</text><path d='M135 37 L175 37' stroke='#C34113' stroke-width='2' marker-end='url(#ag-blue2)'/><rect x='185' y='10' width='130' height='55' rx='8' fill='#E95420'/><text x='250' y='33' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>GitHub</text><text x='250' y='50' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='10'>Issue OIDC token</text><path d='M315 37 L355 37' stroke='#C34113' stroke-width='2' marker-end='url(#ag-blue2)'/><rect x='365' y='10' width='130' height='55' rx='8' fill='#E95420'/><text x='430' y='33' text-anchor='middle' fill='#fff' font-size='12' font-weight='bold'>Cloud IdP</text><text x='430' y='50' text-anchor='middle' fill='rgba(255,255,255,0.8)' font-size='10'>Exchange token</text><path d='M495 37 L535 37' stroke='#E95420' stroke-width='2' marker-end='url(#ag-bright2)'/><rect x='545' y='10' width='145' height='55' rx='8' fill='var(--bg-secondary)' stroke='#E95420' stroke-width='2'/><text x='617' y='33' text-anchor='middle' fill='#E95420' font-size='12' font-weight='bold'>Cloud Resource</text><text x='617' y='50' text-anchor='middle' fill='rgba(233,84,32,0.8)' font-size='10'>Access granted</text></svg>",
  "sections": [
    {
      "title": "GitOps Principles",
      "description": "<p>GitOps has four core principles: declarative configuration stored in Git, desired state reconciled automatically, idempotent and deterministic operations, and continuous observation of actual state. Systems like ArgoCD and Flux CD continuously compare the Git state with cluster state and correct drift. The operator watches the repo, diffs manifests, and applies changes to reach the desired state.</p>"
    },
    {
      "title": "Terraform + Actions",
      "description": "<p>Combine Terraform with GitHub Actions for infrastructure provisioning. Use the <code>hashicorp/setup-terraform</code> action with a remote backend (S3, AzureRM, GCS). Store state remotely with locking. Run <code>terraform plan</code> in PRs with <code>tfsec</code> for security scanning, and <code>terraform apply</code> on merge to main. Use OIDC to authenticate Terraform to cloud providers without static credentials.</p><pre><code>jobs:\n  terraform:\n    permissions:\n      id-token: write\n      contents: read\n    steps:\n      - uses: hashicorp/setup-terraform@v3\n      - run: terraform plan -out=tfplan</code></pre>"
    },
    {
      "title": "OIDC Auth",
      "description": "<p>OpenID Connect replaces long-lived cloud secrets with short-lived tokens. GitHub Actions requests an OIDC token from <code>https://token.actions.githubusercontent.com</code>. The cloud provider (AWS, Azure, GCP) validates the token against a configured trust policy. Claims include <code>sub</code> (repo owner/repo), <code>ref</code> (branch/tag), and <code>actor</code> (username).</p><pre><code># AWS trust policy condition\n\"Condition\": {\n  \"StringLike\": {\n    \"token.actions.githubusercontent.com:sub\": \"repo:org/repo:*\"\n  }\n}</code></pre>"
    },
    {
      "title": "ArgoCD Integration",
      "description": "<p>ArgoCD integrates with GitHub via the <code>argocd-image-updater</code> and webhook triggers. Configure a webhook from GitHub to ArgoCD to trigger sync on push. Use ApplicationSets for multi-environment deployments with <code>generators</code> for Git file, cluster, or pull request generation. Enable auto-sync with <code>selfHeal</code> and <code>prune</code> for drift correction.</p><pre><code>apiVersion: argoproj.io/v1alpha1\nkind: Application\nmetadata:\n  name: my-app\nspec:\n  source:\n    repoURL: https://github.com/org/repo.git\n    path: k8s/overlays/prod\n  syncPolicy:\n    automated:\n      prune: true\n      selfHeal: true</code></pre>"
    },
    {
      "title": "Environment Promotion",
      "description": "<p>Promote deployments through dev -> staging -> canary -> prod using Git merges. Each environment has a branch or directory (<code>k8s/overlays/dev</code>, <code>k8s/overlays/prod</code>). Use PRs for environment promotion with required approvals. Deployment frequency metrics track lead time from merge to production sync. GitHub Actions <code>environment</code> blocks tie deployment approvals to environments.</p><pre><code>jobs:\n  deploy-prod:\n    environment: production\n    steps:\n      - run: kubectl apply -f k8s/overlays/prod</code></pre>"
    }
  ],
  "details": "<p>GitOps shifts from imperative (run command) to declarative (describe desired state) operations. The reconciliation loop is the core abstraction: the operator (ArgoCD/Flux) polls or watches the Git repository for changes, diffs the desired state against the cluster/cloud state, and applies the diff. Any manual change to the cluster is overwritten on the next sync (drift correction). OIDC authentication is critical for security — GitHub Actions receives an OIDC token (JWT) from the GitHub OIDC provider, which includes claims like <code>repository</code>, <code>ref</code>, <code>sha</code>, and <code>workflow</code>. The cloud provider's trust policy validates these claims, returning a cloud access token that is short-lived (typically 1 hour). No secrets to store in GitHub Secrets. For multi-environment GitOps, structure repos with <code>kustomize</code> overlays or Helm value files per environment. Use ApplicationSets in ArgoCD with the Git generator to automatically create Applications for each directory or branch. Environment promotion should follow your release strategy: blue-green or canary via Flagger or Argo Rollouts. Monitor deployment events via the GitHub Deployments API — each deployment creates a deployment event visible in the UI with environment, commit, and status.</p>",
  "tags": [
    "staff+",
    "enterprise",
    "gitops",
    "deployments"
  ],
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>kubectl apply -k k8s/overlays/prod</code> — apply Kustomize overlay from Git.</li><li><code>argocd app sync my-app</code> — trigger ArgoCD sync from CLI.</li><li><code>terraform plan -out=tfplan</code> — plan infrastructure changes in PR.</li></ul><h4>Staff+ Patterns</h4><ul><li>OIDC between GitHub Actions and cloud providers replaces all static secrets — <code>id-token: write</code> permission, cloud trust policy validates claims.</li><li>ArgoCD ApplicationSets with Git generator auto-create Apps for each directory or branch.</li><li>Environment promotion via Git merges: dev branch → staging branch → prod tag. Each promotion runs a PR with approvals.</li></ul><h4>Watch Outs</h4><ul><li>Drift correction (auto-sync with <code>selfHeal</code>) can overwrite emergency manual fixes — use sparingly.</li><li>Terraform state locking is critical in CI — without it, concurrent applies corrupt state.</li><li>OIDC token claims must be specific — <code>repo:org/repo:*</code> is too broad. Scope to <code>ref:refs/heads/main</code>.</li></ul><h4>Interview Angle</h4><p>Netflix/Amazon L6: \"Design a GitOps deployment pipeline for 200 microservices.\" Discuss: environment repos, promotion gates, canary analysis (Flagger/Argo Rollouts), and automated rollback via <code>git revert</code> with the deployment tag as the rollback target.</p>",
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}