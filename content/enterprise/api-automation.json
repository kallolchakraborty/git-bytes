{
  "id": "api-automation",
  "title": "API Automation",
  "category": "Enterprise",
  "subcategory": "Automation",
  "language": "bash",
  "description": "<p>Automating GitHub at scale requires understanding authentication flows, webhook delivery guarantees, and the right API surface for each use case.</p><svg viewBox=\"0 0 580 120\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%;height:auto;display:block;margin:1.5rem 0\"><defs><marker id=\"a1\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#C34113\"/></marker><marker id=\"a2\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#E95420\"/></marker></defs><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0s\" fill=\"freeze\"/><rect x=\"10\" y=\"25\" width=\"120\" height=\"70\" rx=\"8\" fill=\"#C34113\"/><text x=\"70\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">GitHub App</text><text x=\"70\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">Private Key</text></g><line x1=\"130\" y1=\"60\" x2=\"155\" y2=\"60\" stroke=\"#C34113\" stroke-width=\"2\" marker-end=\"url(#a1)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"0.3s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.5s\" fill=\"freeze\"/><rect x=\"160\" y=\"25\" width=\"120\" height=\"70\" rx=\"8\" fill=\"#E95420\"/><text x=\"220\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">JWT Token</text><text x=\"220\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">RS256 Signed</text></g><line x1=\"280\" y1=\"60\" x2=\"305\" y2=\"60\" stroke=\"#C34113\" stroke-width=\"2\" marker-end=\"url(#a1)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"0.8s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"1.0s\" fill=\"freeze\"/><rect x=\"310\" y=\"25\" width=\"120\" height=\"70\" rx=\"8\" fill=\"#C34113\"/><text x=\"370\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Install Token</text><text x=\"370\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">Expires 1h</text></g><line x1=\"430\" y1=\"60\" x2=\"455\" y2=\"60\" stroke=\"#E95420\" stroke-width=\"2\" marker-end=\"url(#a2)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"1.3s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"1.5s\" fill=\"freeze\"/><rect x=\"460\" y=\"25\" width=\"110\" height=\"70\" rx=\"8\" fill=\"none\" stroke=\"#E95420\" stroke-width=\"2\"/><text x=\"515\" y=\"57\" text-anchor=\"middle\" fill=\"#E95420\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">GitHub API</text><text x=\"515\" y=\"75\" text-anchor=\"middle\" fill=\"#E95420\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\" opacity=\"0.7\">REST + GraphQL</text></g></svg>",
  "sections": [
    {
      "title": "GitHub Apps vs OAuth Apps",
      "description": "<p>GitHub Apps are first-class actors with granular permissions, installation scoping, and rate limit isolation. OAuth Apps act on behalf of a user with a single token. Prefer GitHub Apps for automation: they authenticate via JWT (RS256 signed with app private key), exchange for an installation access token (1h expiry), and scope requests to specific repositories.</p><pre><code># Generate JWT\npayload = {\n  iat: now - 60, exp: now + 600, iss: app_id\n}\ntoken = jwt.encode(payload, private_key, algorithm=\"RS256\")\n# Exchange for install token\ncurl -X POST /app/installations/{id}/access_tokens \\\n  -H \"Authorization: Bearer $jwt\"</code></pre>"
    },
    {
      "title": "Checks API",
      "description": "<p>The Checks API lets CI systems create check runs and suites that appear directly in pull requests. Each check run has a name, status (queued/in_progress/completed), conclusion (success/failure/neutral/cancelled), and annotations at the file+line level. Use <code>POST /repos/{owner}/{repo}/check-runs</code> with <code>output.summary</code> and <code>output.annotations</code> for rich feedback.</p><pre><code>curl -X POST /repos/org/repo/check-runs \\\n  -H \"Accept: application/vnd.github+json\" \\\n  -d '{\"name\":\"lint\",\"head_sha\":\"$SHA\",\"status\":\"completed\",\"conclusion\":\"success\",\"output\":{\"title\":\"ESLint\",\"summary\":\"0 errors\"}}'</code></pre>"
    },
    {
      "title": "Webhook Delivery",
      "description": "<p>Webhooks deliver event payloads to configurable URLs with a retry mechanism. Upon delivery failure (non-2xx), GitHub retries up to 3 times with exponential backoff (~30s, ~2min, ~5min). Each delivery includes a unique <code>x-github-delivery</code> ID and the <code>x-hub-signature-256</code> HMAC for verification. Set up a dead letter queue for hardened delivery guarantees.</p><pre><code># Verify payload signature\nconst sig = crypto\n  .createHmac('sha256', secret)\n  .update(payload)\n  .digest('hex')\nassert(sig === header)</code></pre>"
    },
    {
      "title": "Batch GraphQL",
      "description": "<p>GraphQL enables batched queries that replace multiple REST calls. Use aliases, fragments, and pagination via <code>first</code>/<code>after</code> cursors. The <code>nodes</code> pattern is efficient for bulk operations. Rate limit is based on node count (5000 points per hour, query cost = <code>cost</code> field in response).</p><pre><code>query repos($org: String!) {\n  organization(login: $org) {\n    repositories(first: 100) {\n      nodes { name defaultBranchRef { name } }\n      pageInfo { hasNextPage endCursor }\n    }\n  }\n}</code></pre>"
    },
    {
      "title": "Internal Tooling",
      "description": "<p>Build internal developer tooling with GitHub's API surface. Patterns include: <code>gh api</code> in shell scripts, Octokit SDK wrappers, and custom Actions. Use conditional requests with <code>If-None-Match</code> / <code>ETag</code> to reduce bandwidth. Cache API responses with TTLs respecting <code>x-ratelimit-remaining</code> headers.</p><pre><code>gh api repos/org/repo --jq '.default_branch'\ngh api --paginate /orgs/org/repos --jq '.[].name'</code></pre>"
    }
  ],
  "details": "<p>API automation at scale demands careful authentication strategy. GitHub Apps are the gold standard for automation — they authenticate as a bot, scope to installations, and have dedicated rate limits (5000/hour per app, not per user). The JWT flow: generate a JWT signed with your app's private key (RS256, 10 minute max expiry), POST to <code>/app/installations/{id}/access_tokens</code>, and use the returned token for API calls. Tokens expire after 1 hour; re-authenticate by generating a new JWT. For webhook reliability, configure a webhook with a secret, verify HMAC signatures, and respond with 200 within 10 seconds to avoid timeouts. Use the <code>/app/hook/deliveries</code> endpoint to inspect recent deliveries and replay failed ones. GraphQL is preferable for bulk queries — a single <code>repositories</code> query with 100 nodes costs 1 point (plus 1 per node). Always handle pagination with cursors — REST returns <code>Link</code> headers, GraphQL uses <code>pageInfo</code>. For internal tooling, the <code>gh</code> CLI's <code>--paginate</code> flag auto-fetches all pages. Build a CI/CD bridge using the Checks API: each pipeline step maps to a check run with annotations pointing to the exact line of failure.</p>",
  "tags": [
    "staff+",
    "enterprise",
    "api",
    "automation"
  ],
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>gh api /repos/{owner}/{repo}/check-runs</code> — create check runs with file-level annotations.</li><li><code>gh api --paginate /orgs/{org}/repos --jq '.[].name'</code> — list all repos with pagination.</li><li><code>gh api /repos/{owner}/{repo}/git/refs</code> — interact with Git refs directly.</li></ul><h4>Staff+ Patterns</h4><ul><li>Prefer GitHub Apps over OAuth Apps for automation — granular permissions, installation scoping, dedicated rate limits.</li><li>Use GraphQL for bulk queries — a single <code>repositories</code> query with 100 nodes replaces 100 REST calls.</li><li>Webhook verification: validate <code>x-hub-signature-256</code> HMAC before processing payloads.</li></ul><h4>Watch Outs</h4><ul><li>GitHub App installation tokens expire after 1 hour — regenerate via JWT re-authentication.</li><li>REST API pagination (page/per_page) is O(n) for deep pages. GraphQL cursor pagination is O(1).</li><li>Rate limits: REST 5000/hr per user, GitHub App 5000/hr per installation. GraphQL uses point-based costing.</li></ul><h4>Interview Angle</h4><p>GitHub L6: \"Design a CI bridge using the Checks API.\" Key: each pipeline step maps to a check run with annotations pointing to the exact file + line of failure. Use <code>POST /repos/{owner}/{repo}/check-runs</code> with <code>output.annotations</code> for rich inline feedback in the PR.</p>",
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}
