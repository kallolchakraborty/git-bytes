{
  "id": "deployment-pipelines",
  "title": "Deployment Pipelines",
  "category": "DevOps",
  "subcategory": "Deployment",
  "language": "yaml",
  "description": "<p>Design safe deployment pipelines with environment promotion, approval gates, rollback strategies, and release automation. Covers blue-green, canary, and rolling deploy patterns at scale.</p><svg viewBox=\"0 0 550 120\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%;height:auto;display:block;margin:1.5rem 0\"><defs><marker id=\"h1\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#C34113\"/></marker><marker id=\"h2\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#E95420\"/></marker></defs><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0s\" fill=\"freeze\"/><rect x=\"10\" y=\"25\" width=\"90\" height=\"70\" rx=\"8\" fill=\"#C34113\"/><text x=\"55\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">dev</text><text x=\"55\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">auto-deploy</text></g><line x1=\"100\" y1=\"60\" x2=\"120\" y2=\"60\" stroke=\"#C34113\" stroke-width=\"2\" marker-end=\"url(#h1)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"0.3s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.5s\" fill=\"freeze\"/><rect x=\"125\" y=\"25\" width=\"90\" height=\"70\" rx=\"8\" fill=\"#E95420\"/><text x=\"170\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">staging</text><text x=\"170\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">auto-deploy</text></g><line x1=\"215\" y1=\"60\" x2=\"235\" y2=\"60\" stroke=\"#E95420\" stroke-width=\"2\" marker-end=\"url(#h2)\" stroke-dasharray=\"20\" stroke-dashoffset=\"20\"><animate attributeName=\"stroke-dashoffset\" from=\"20\" to=\"0\" dur=\"0.4s\" begin=\"0.7s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.9s\" fill=\"freeze\"/><rect x=\"240\" y=\"30\" width=\"50\" height=\"60\" rx=\"4\" fill=\"#DA3633\"/><text x=\"265\" y=\"67\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"11\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">GATE</text></g><line x1=\"290\" y1=\"60\" x2=\"310\" y2=\"60\" stroke=\"#DA3633\" stroke-width=\"2\" marker-end=\"url(#h1)\" stroke-dasharray=\"20\" stroke-dashoffset=\"20\"><animate attributeName=\"stroke-dashoffset\" from=\"20\" to=\"0\" dur=\"0.4s\" begin=\"1.1s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"1.3s\" fill=\"freeze\"/><rect x=\"315\" y=\"25\" width=\"90\" height=\"70\" rx=\"8\" fill=\"#F0883E\"/><text x=\"360\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">canary</text><text x=\"360\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">10% traffic</text></g><line x1=\"405\" y1=\"60\" x2=\"425\" y2=\"60\" stroke=\"#F0883E\" stroke-width=\"2\" marker-end=\"url(#h2)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"1.5s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"1.7s\" fill=\"freeze\"/><rect x=\"430\" y=\"25\" width=\"110\" height=\"70\" rx=\"8\" fill=\"#238636\"/><text x=\"485\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">prod</text><text x=\"485\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">100% traffic</text></g></svg>",
  "sections": [
    {
      "title": "Environments",
      "description": "<p>GitHub Environments provide protection rules, secret scoping, and deployment tracking. Each environment can require reviewers, wait timers, and branch restrictions before deployment proceeds.</p>",
      "codeBlock": "# workflow.yml — environment definition\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    environment:\n      name: production\n      url: https://app.example.com\n    steps:\n      - run: ./deploy.sh\n\n# Environment protection via GitHub UI:\n# Settings → Environments → production\n# Required reviewers: 2\n# Wait timer: 30 minutes\n# Deployment branches: main\n# Secret isolation:\n#   production → PROD_API_KEY\n#   staging → STAGING_API_KEY"
    },
    {
      "title": "Approval Gates",
      "description": "<p>Gate promotions with required reviews, status checks, and environment protection rules. Combine with <code>workflow_dispatch</code> inputs for manual control over promotion parameters.</p>",
      "codeBlock": "jobs:\n  promote-to-canary:\n    environment: canary\n    runs-on: ubuntu-latest\n    steps:\n      - run: ./promote.sh --percentage 10\n\n  promote-to-prod:\n    needs: promote-to-canary\n    environment:\n      name: production\n      url: ${{ steps.deploy.outputs.url }}\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/github-script@v7\n        id: deploy\n        with:\n          script: |\n            // add deployment status\n            await github.rest.repos.createDeploymentStatus({\n              ...context.repo,\n              deployment_id: process.env.DEPLOYMENT_ID,\n              state: 'success',\n              environment_url: 'https://app.example.com'\n            })"
    },
    {
      "title": "Rollback Strategies",
      "description": "<p>Rollback via <code>git revert</code> and redeploy, or use <code>workflow_dispatch</code> with a target version input. Store deployment metadata in deployment statuses and environment variables for traceability.</p>",
      "codeBlock": "name: Rollback\non:\n  workflow_dispatch:\n    inputs:\n      target-version:\n        description: 'Git SHA or tag to rollback to'\n        required: true\n\njobs:\n  rollback:\n    runs-on: ubuntu-latest\n    environment: production\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ inputs.target-version }}\n      - run: |\n          echo \"Rolling back to ${{ github.event.inputs.target-version }}\"\n          ./deploy.sh\n      - uses: actions/github-script@v7\n        with:\n          script: |\n            await github.rest.repos.createCommitComment({\n              ...context.repo,\n              commit_sha: context.sha,\n              body: 'Rollback to ${{ inputs.target-version }}'\n            })"
    },
    {
      "title": "Blue-Green & Canary",
      "description": "<p>Blue-green deploys maintain two full environments with instant cutover. Canary releases gradually shift traffic while monitoring error rates. Both require a load balancer or service mesh integration.</p>",
      "codeBlock": "# Blue-green deploy with AWS ECS\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - id: deploy\n        run: |\n          aws ecs update-service \\\n            --cluster prod \\\n            --service app \\\n            --task-definition app:${{ github.sha }} \\\n            --network-config \"awsvpcConfiguration={subnets=[subnet-abc]}\"\n      - id: swap\n        run: |\n          aws ecs update-service \\\n            --cluster prod \\\n            --service app-blue \\\n            --task-definition app:latest\n          # DNS or ALB switch for instant cutover\n\n# Canary with flag-based routing\n      - run: |\n          # Deploy to 5% of instances\n          kubectl set image deployment/app \\\n            app=myapp:${{ github.sha }} \\\n            --record\n          kubectl scale deployment/app-canary --replicas=1\n          # Monitor for 10 minutes\n          sleep 600\n          # Ramp to 50%\n          kubectl scale deployment/app-canary --replicas=5\n          sleep 600\n          # Full rollout\n          kubectl scale deployment/app --replicas=10"
    },
    {
      "title": "Release Automation",
      "description": "<p>Automate semantic versioning, changelog generation, and GitHub Releases with <code>actions/github-script</code>, <code>ncipollo/release-action</code>, and conventional commits parsing for full GitOps workflows.</p>",
      "codeBlock": "jobs:\n  release:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: write\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - id: tag\n        run: |\n          # auto-increment semver from tags\n          LATEST=$(git tag -l 'v*' | sort -V | tail -1)\n          NEW_VERSION=v$(echo $LATEST | awk -F. '{$NF+=1; print}' OFS='.')\n          git tag $NEW_VERSION\n          git push origin $NEW_VERSION\n          echo \"version=$NEW_VERSION\" >> $GITHUB_OUTPUT\n      - uses: ncipollo/release-action@v1\n        with:\n          tag: ${{ steps.tag.outputs.version }}\n          generateReleaseNotes: true\n          makeLatest: true"
    }
  ],
  "details": "<p>Deployment pipelines at scale require progressive exposure and safety mechanisms. GitHub Actions environments map to deployment targets with branch restrictions, secret isolation, and required reviewers — enabling compliance without leaving the CI/CD platform. For promotion flows, model <strong>dev → staging → canary → prod</strong> where canary and prod gates require manual approval. Blue-green deployments provide instant rollback via traffic rerouting but double infrastructure cost. Canary releases incrementally shift traffic using weighted load balancer rules, automatically rolling back if error rates spike. Rolling updates replace instances one-by-one — safest for stateless services but slowest during rollback. For rollback automation, run <code>git revert</code> against the deployment commit and push to the protected branch, triggering the same deploy workflow with the prior version. Store deployment IDs, timestamps, and commit SHAs in GitHub Deployment Statuses for full audit trail.</p>",
  "tags": [
    "devops",
    "deployment",
    "pipelines",
    "staff+"
  ],
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>environment: production</code> — define deployment target with approvals.</li><li><code>needs: [build, test]</code> — sequential deployment stages.</li><li><code>if: github.ref == 'refs/heads/main'</code> — deploy only from main.</li><li><code>rollback: true</code> via <code>git revert</code> + deploy revert commit.</li></ul><h4>Staff+ Patterns</h4><ul><li>Use <strong>progressive delivery</strong>: deploy to staging, run integration tests, promote to canary (1%), validate, roll out gradually. Each stage is a separate environment with approval gates.</li><li>Use <code>deployment</code> events with <code>environment</code> for audit trail — every deploy is recorded in the GitHub API with commit SHA, timestamp, and actor.</li></ul><h4>Watch Outs</h4><ul><li>Rollback via <code>git revert</code> is safe; <code>git reset --hard</code> on main is catastrophic for collaborators. Always revert.</li><li>Database migrations can't be reverted by code rollback alone. Use <code>git revert</code> + a compensating migration.</li></ul><h4>Interview Angle</h4><p>Netflix L7 asks this directly. Key: design for zero-downtime deployments (blue/green or canary), automatable rollback, and observability during rollout. The deploy should be boring — a script that runs the same way every time.</p>",
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}
