{
  "id": "actions-cost-optimization",
  "title": "Actions Cost Optimization",
  "category": "DevOps",
  "subcategory": "Cost",
  "language": "yaml",
  "description": "<p>Strategies to minimize GitHub Actions billing costs through caching, runner selection, concurrency management, and cost monitoring. Analyze compute vs storage vs transfer cost drivers.</p><svg viewBox=\"0 0 500 100\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%;height:auto;display:block;margin:1.5rem 0\"><defs><marker id=\"h1\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#C34113\"/></marker><marker id=\"h2\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#E95420\"/></marker></defs><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0s\" fill=\"freeze\"/><rect x=\"10\" y=\"15\" width=\"130\" height=\"70\" rx=\"8\" fill=\"#C34113\"/><text x=\"75\" y=\"47\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Compute</text><text x=\"75\" y=\"65\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">~65% of bill</text></g><line x1=\"140\" y1=\"50\" x2=\"165\" y2=\"50\" stroke=\"#C34113\" stroke-width=\"2\" marker-end=\"url(#h1)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"0.3s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.5s\" fill=\"freeze\"/><rect x=\"175\" y=\"15\" width=\"130\" height=\"70\" rx=\"8\" fill=\"#E95420\"/><text x=\"240\" y=\"47\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Storage</text><text x=\"240\" y=\"65\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">~30% of bill</text></g><line x1=\"305\" y1=\"50\" x2=\"330\" y2=\"50\" stroke=\"#E95420\" stroke-width=\"2\" marker-end=\"url(#h2)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"0.7s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.9s\" fill=\"freeze\"/><rect x=\"340\" y=\"15\" width=\"130\" height=\"70\" rx=\"8\" fill=\"#F0883E\"/><text x=\"405\" y=\"47\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Transfer</text><text x=\"405\" y=\"65\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">~5% of bill</text></g></svg>",
  "sections": [
    {
      "title": "Pricing Model",
      "description": "<p>GitHub Actions billing is based on compute minutes (per OS), storage (artifacts + caches), and data transfer. Linux minutes are the baseline; macOS and Windows incur 10× and 2× multipliers respectively.</p>",
      "codeBlock": "# GitHub Actions billing rates (public cloud)\n# Linux:    $0.008/min  (1×)\n# Windows:  $0.016/min  (2×)\n# macOS:    $0.080/min  (10×)\n#\n# Storage: $0.25/GB/month after 1 GB free (artifacts + caches)\n# Transfer: $0.50–$0.90/GB depending on region\n#\n# Free tier (public repos: unlimited)\n# Free tier (private repos):\n#   2,000 min/month Linux\n#   Equivalent to 200 min macOS or 1,000 min Windows\n#   500 MB storage\n#   1 GB transfer\n#\n# Compute cost = min_used × multiplier × $0.008\n# Example: 500 min Windows = 500 × 2 × $0.008 = $8.00"
    },
    {
      "title": "Caching Strategies",
      "description": "<p>Use <code>actions/cache</code> and <code>actions/cache/save</code>/<code>restore</code> to persist dependencies and build outputs across workflow runs. Cache keys use hash inputs to invalidate on change. Target &gt;80% hit rate.</p>",
      "codeBlock": "jobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Cache node_modules\n        id: cache-npm\n        uses: actions/cache@v4\n        with:\n          path: ~/.npm\n          key: npm-${{ runner.os }}-${{ hashFiles('package-lock.json') }}\n          restore-keys: |\n            npm-${{ runner.os }}-\n            npm-\n\n      - name: Cache build output\n        uses: actions/cache@v4\n        with:\n          path: |\n            dist\n            .next\n          key: build-${{ github.sha }}\n          restore-keys: |\n            build-${{ github.ref }}-\n            build-\n\n      - if: steps.cache-npm.outputs.cache-hit != 'true'\n        run: npm ci"
    },
    {
      "title": "Runner Selection",
      "description": "<p>Match runner size to job requirements. Use smaller runners for linting, larger runners for builds. Self-hosted runners eliminate per-minute costs entirely but require infrastructure management.</p>",
      "codeBlock": "jobs:\n  lint:\n    runs-on: ubuntu-latest  # free tier or cheap\n    steps:\n      - run: npm run lint\n\n  test:\n    strategy:\n      matrix:\n        os: [ubuntu-latest]  # avoid macOS unless needed\n    runs-on: ${{ matrix.os }}\n    steps:\n      - run: npm test\n\n  build:\n    runs-on:\n      group: larger-runner  # org-level larger runner\n      labels: [self-hosted, linux, x64]\n    # Self-hosted: $0/min compute cost\n    # But: infrastructure, maintenance, network egress\n\n  e2e:\n    runs-on: macos-latest  # only if macOS required\n    # $0.08/min — use sparingly\n    steps:\n      - run: npm run test:e2e\n\n# Label routing: assign runner pools per job criticality\n# high-priority → dedicated runner group\n# low-priority → shared group with queue"
    },
    {
      "title": "Concurrency Management",
      "description": "<p>Use concurrency groups to prevent redundant builds (cancel duplicate pushes) and limit parallel jobs. Set <code>max-parallel</code> on matrix strategies to control burst spend.</p>",
      "codeBlock": "name: CI\non:\n  push:\n    branches-ignore:\n      - 'dependabot/**'  # skip dependabot if desired\n\nconcurrency:\n  group: ${{ github.workflow }}-${{ github.ref }}\n  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n\njobs:\n  test:\n    strategy:\n      matrix:\n        os: [ubuntu-latest, windows-latest]\n        node: [18, 20, 22]\n      max-parallel: 4  # max concurrent matrix jobs\n    runs-on: ${{ matrix.os }}\n    timeout-minutes: 30  # prevent runaway jobs\n    steps:\n      - run: npm ci && npm test\n\n# Further controls:\n# - timeout-minutes: hard limit per job\n# - scheduled workflows: off-peak hours\n# - path filters: only run on relevant changes\n#   on:\n#     push:\n#       paths:\n#         - 'src/**'\n#         - '*.json'"
    },
    {
      "title": "Cost Monitoring",
      "description": "<p>Track costs via the GitHub billing API, organization usage graphs, and webhook events. Set budgets and alerts for unexpected spend spikes. Use <code>github-script</code> to automate cost reporting.</p>",
      "codeBlock": "# Get organization usage\ngh api /orgs/{org}/settings/billing/actions\n\n# Response includes:\n# {\n#   \"total_minutes_used\": 15000,\n#   \"total_paid_minutes_used\": 13000,\n#   \"included_minutes\": 2000,\n#   \"minutes_used_breakdown\": {\n#     \"UBUNTU\": 10000,\n#     \"MACOS\": 2000,\n#     \"WINDOWS\": 3000\n#   }\n# }\n\n# Automated cost report (weekly workflow):\nname: Cost Report\non:\n  schedule:\n    - cron: '0 9 * * 1'  # Monday 9 AM\njobs:\n  report:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/github-script@v7\n        with:\n          script: |\n            const billing = await github.rest.actions.getBillingUsage({\n              owner: context.repo.owner\n            });\n            const { total_minutes_used, minutes_used_breakdown } = billing.data;\n            const estimatedCost =\n              (minutes_used_breakdown.UBUNTU || 0) * 0.008 +\n              (minutes_used_breakdown.WINDOWS || 0) * 0.016 +\n              (minutes_used_breakdown.MACOS || 0) * 0.080;\n            // Post to Slack / Teams / email"
    }
  ],
  "details": "<p>GitHub Actions cost optimization requires visibility into three dimensions: <strong>compute</strong>, <strong>storage</strong>, and <strong>transfer</strong>. Compute dominates (~65% of bill) with Linux at $0.008/min as baseline — macOS at 10× should only be used for iOS/macOS-specific builds. Caching is the highest-leverage optimization: a well-configured cache with &gt;80% hit rate can eliminate 60-70% of dependency install time. Use <code>hashFiles()</code> on lockfiles for dependency caches and build artifact caching with <code>restore-keys</code> for partial-match fallback. Cache eviction follows LRU with a 10 GB repository limit — prioritize key quality over cache size. Self-hosted runners eliminate per-minute billing entirely but shift cost to infrastructure (EC2, VMs, Kubernetes) and operational overhead (updates, patching, networking). Concurrency groups with <code>cancel-in-progress</code> prevent wasted minutes from superseded pushes, and <code>max-parallel</code> caps matrix job burst. Path filtering on workflow triggers ensures compute is only consumed for relevant changes. Monitor costs weekly via the billing API and set up automated alerts for anomalous spend spikes.</p>",
  "tags": [
    "devops",
    "actions",
    "cost",
    "staff+"
  ],
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>concurrency: group: ci group: ci cancel-in-progress: true</code> — cancel redundant runs.</li><li><code>actions/cache@v4</code> — cache dependencies across runs.</li><li><code>self-hosted</code> runner — free compute, no per-minute billing.</li></ul><h4>Staff+ Patterns</h4><ul><li>Use <code>paths-ignore</code> and <code>paths-filter</code> to skip CI on docs-only or non-source changes — biggest quick cost save.</li><li>Cache <code>~/.npm</code>, <code>~/.cache/pip</code>, and <code>node_modules</code> with language-specific keys including <code>hashFiles</code> for exact restoration.</li></ul><h4>Watch Outs</h4><ul><li>Self-hosted runners still cost maintenance time. Factor in admin overhead vs. per-minute savings.</li><li>Cache restore is not guaranteed (cache eviction, key mismatch). Always fall back to <code>npm ci</code>.</li></ul><h4>Interview Angle</h4><p>Cost optimization is a Staff+ concern — show you think about the bottom line. Discuss trade-offs: faster CI (more parallelism) vs cheaper CI (conservative parallelism, caching, self-hosted).</p>",
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}
