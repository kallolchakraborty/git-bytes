{
  "id": "graphql-api",
  "title": "GitHub GraphQL API",
  "category": "GitHub",
  "subcategory": "Advanced",
  "language": "graphql",
  "description": "<p>The GitHub GraphQL API enables precise data fetching — request exactly what you need in a single round trip. Unlike REST's waterfall of N+1 requests, GraphQL resolves nested resources in one query tree traversal. This guide covers schema design, mutations, cursor-based pagination, and real-world query patterns.</p><svg viewBox=\"0 0 550 160\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%;height:auto;display:block;margin:1.5rem 0\" font-family=\"Ubuntu,sans-serif\"><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0s\" fill=\"freeze\"/><text x=\"137\" y=\"20\" text-anchor=\"middle\" fill=\"#DA3633\" font-size=\"14\" font-weight=\"700\">REST Waterfall</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.8s\" fill=\"freeze\"/><text x=\"412\" y=\"20\" text-anchor=\"middle\" fill=\"#238636\" font-size=\"14\" font-weight=\"700\">GraphQL</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.2s\" fill=\"freeze\"/><rect x=\"40\" y=\"35\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#DA3633\"/><text x=\"87\" y=\"53\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"9\" font-weight=\"700\">GET /users</text></g><line x1=\"87\" y1=\"63\" x2=\"87\" y2=\"76\" stroke=\"#DA3633\" stroke-width=\"2\" stroke-dasharray=\"15\" stroke-dashoffset=\"15\"><animate attributeName=\"stroke-dashoffset\" from=\"15\" to=\"0\" dur=\"0.2s\" begin=\"0.5s\" fill=\"freeze\"/></line><polygon points=\"82,76 87,82 92,76\" fill=\"#DA3633\" opacity=\"0\"><animate attributeName=\"opacity\" to=\"1\" dur=\"0.1s\" begin=\"0.7s\" fill=\"freeze\"/></polygon><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.55s\" fill=\"freeze\"/><rect x=\"40\" y=\"82\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#DA3633\"/><text x=\"87\" y=\"100\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"9\" font-weight=\"700\">GET /repos</text></g><line x1=\"87\" y1=\"110\" x2=\"87\" y2=\"123\" stroke=\"#DA3633\" stroke-width=\"2\" stroke-dasharray=\"15\" stroke-dashoffset=\"15\"><animate attributeName=\"stroke-dashoffset\" from=\"15\" to=\"0\" dur=\"0.2s\" begin=\"0.85s\" fill=\"freeze\"/></line><polygon points=\"82,123 87,129 92,123\" fill=\"#DA3633\" opacity=\"0\"><animate attributeName=\"opacity\" to=\"1\" dur=\"0.1s\" begin=\"1.05s\" fill=\"freeze\"/></polygon><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.9s\" fill=\"freeze\"/><rect x=\"40\" y=\"129\" width=\"95\" height=\"28\" rx=\"6\" fill=\"#DA3633\"/><text x=\"87\" y=\"147\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"9\" font-weight=\"700\">GET /issues</text></g><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.35s\" begin=\"0.2s\" fill=\"freeze\"/><rect x=\"315\" y=\"55\" width=\"95\" height=\"60\" rx=\"8\" fill=\"#238636\"/><text x=\"362\" y=\"78\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"9\" font-weight=\"700\">query {}</text><text x=\"362\" y=\"97\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"9\">1 round trip</text></g></svg>",
  "sections": [
    {
      "title": "GraphQL vs REST",
      "description": "<p>GraphQL eliminates N+1 requests by allowing nested resource fetching in a single query. Where REST requires <code>GET /users/:user</code> &rarr; <code>GET /repos/:owner/:repo</code> &rarr; <code>GET /issues/:id</code>, GraphQL resolves <code>query { user { repositories(first:10) { issues(first:10) { title } } } }</code> in one round trip.</p><p>Trade-offs: GraphQL responses cannot be cached at the HTTP level (always POST, always 200), responses are larger per query, and the learning curve is steeper. REST is better for simple CRUD, caching, and tooling with standard HTTP semantics.</p>"
    },
    {
      "title": "Schema & Queries",
      "description": "<p>The GraphQL schema defines the shape of available data. Root types: <code>Query</code> (read), <code>Mutation</code> (write), <code>Subscription</code> (real-time). Connections use Relay-style pagination with <code>edges { node { ... } }</code> structure.</p><p>Explore the schema interactively at <code>https://docs.github.com/en/graphql/overview/explorer</code> or use the built-in GraphQL Explorer. The schema is self-documenting via introspection queries.</p>"
    },
    {
      "title": "Mutations",
      "description": "<p>Mutations modify data on GitHub. Every mutation returns the mutated object so you can confirm the result in the same request. Mutations follow the pattern <code>mutation { actionName(input: { ... }) { clientMutationId | field } }</code>.</p><p>All mutations require the <code>mutate</code> scope on your token. Common mutations include <code>addComment</code>, <code>updateIssue</code>, <code>mergePullRequest</code>, <code>createRepository</code>, and <code>requestReviews</code>.</p>"
    },
    {
      "title": "Cursor Pagination",
      "description": "<p>All list fields in the GraphQL API use cursor-based pagination via the Relay Connection spec. Key arguments: <code>first</code>/<code>last</code> (number of items), <code>after</code>/<code>before</code> (cursor string). The <code>pageInfo</code> object provides <code>hasNextPage</code>, <code>hasPreviousPage</code>, <code>startCursor</code>, and <code>endCursor</code>.</p><p>Cursors are opaque, stable strings that don't change even when new items are added to the collection — ideal for real-time data where page-based offsets would shift.</p>"
    },
    {
      "title": "Real-World Queries",
      "description": "<p>Real-world patterns include fetching open PRs with review status, checking mergeability before merging, getting issue counts by label, and fetching repository metadata with language breakdowns. Always request only the fields you need to minimize response size and cost.</p><p>GraphQL node limits default to 500,000 nodes per query. Fragment composition and aliased queries can help stay within limits while maximizing data fetched.</p>"
    }
  ],
  "details": "<p>GitHub's GraphQL API is powered by the <code>/graphql</code> endpoint and accepts POST requests with a JSON body containing <code>query</code> (the GraphQL query string) and optional <code>variables</code> (a JSON object mapping variable names to values).</p><h3>Query Structure</h3><p>A query begins with the operation type (<code>query</code>, <code>mutation</code>, or <code>subscription</code>), an optional operation name, and the selection set. Connections are always wrapped in <code>edges { node { ... } }</code> with <code>pageInfo</code> for navigation. Use GraphQL fragments (<code>fragment X on Y</code>) to reuse field sets across queries.</p><h3>Rate Limiting</h3><p>GraphQL rate limiting is based on point cost, not request count. Each query is assigned a cost based on the number of nodes traversed, with a default limit of 5000 points per hour for authenticated users. Complex queries are rejected with a 200 response containing an <code>errors</code> array with type <code>RATE_LIMITED</code>.</p><h3>Aliases &amp; Variables</h3><p>Aliases let you request the same field with different arguments in a single query. Variables keep queries clean and reusable — define them in the operation signature with <code>$varName: Type</code> and pass values in the <code>variables</code> JSON object.</p><h3>Code Examples</h3><pre><code class=\"language-graphql\"># Query: open PRs with review status and mergeability\nquery($owner: String!, $repo: String!, $first: Int!) {\n  repository(owner: $owner, name: $repo) {\n    pullRequests(first: $first, states: [OPEN]) {\n      edges {\n        node {\n          title\n          url\n          mergeable\n          reviews(last: 1) {\n            edges {\n              node {\n                state\n                author { login }\n              }\n            }\n          }\n        }\n      }\n      pageInfo {\n        hasNextPage\n        endCursor\n      }\n    }\n  }\n}\n\n# Mutation: add label and comment to an issue\nmutation($issueId: ID!, $labelId: ID!, $comment: String!) {\n  addLabelsToLabelable(input: {\n    labelableId: $issueId,\n    labelIds: [$labelId]\n  }) { clientMutationId }\n  addComment(input: {\n    subjectId: $issueId,\n    body: $comment\n  }) { commentEdge { node { url } } }\n}\n\n# Fragment reuse across queries\nfragment RepoFields on Repository {\n  nameWithOwner\n  description\n  primaryLanguage { name }\n  stargazerCount\n  forkCount\n}\n</code></pre>",
  "tags": [
    "github",
    "api",
    "graphql"
  ],
  "cheatSheet": "<h4>Core Concepts</h4><ul><li><code>query { repository(owner:&quot;o&quot;, name:&quot;r&quot;) { name } }</code> — read data.</li><li><code>mutation { addComment(input:{...}) { ... } }</code> — write data.</li><li>Connections: <code>edges { node { ... } }</code> + <code>pageInfo { endCursor hasNextPage }</code>.</li></ul><h4>Staff+ Patterns</h4><ul><li>Use GraphQL to replace N+1 REST calls: fetch open PRs with review status and CI checks in one query.</li><li>Use fragments for reusable field sets and aliases for multiple queries of the same type.</li></ul><h4>Watch Outs</h4><ul><li>GraphQL always returns 200 — errors are in <code>errors</code> array. Always check for them.</li><li>Point-based rate limiting: complex queries can exhaust your budget in one request. Paginate conservatively.</li></ul><h4>Interview Angle</h4><p>Key trade-off: GraphQL eliminates over/under-fetching but caching is harder (always POST). The Relay connection spec pattern appears in system design interviews for cursor-based pagination.</p>",
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}