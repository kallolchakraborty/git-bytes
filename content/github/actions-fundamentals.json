{
  "id": "actions-fundamentals",
  "title": "GitHub Actions Fundamentals",
  "category": "GitHub",
  "subcategory": "Actions",
  "language": "yaml",
  "description": "<p>GitHub Actions automates software workflows directly in your repository — from CI/CD to issue management and deployment.</p><svg viewBox=\"0 0 550 120\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%;height:auto;display:block;margin:1.5rem 0\"><defs><marker id=\"h1\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#C34113\"/></marker><marker id=\"h2\" markerWidth=\"8\" markerHeight=\"6\" refX=\"8\" refY=\"3\" orient=\"auto\"><polygon points=\"0 0,8 3,0 6\" fill=\"#E95420\"/></marker></defs><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0s\" fill=\"freeze\"/><rect x=\"10\" y=\"25\" width=\"100\" height=\"70\" rx=\"8\" fill=\"#C34113\"/><text x=\"60\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">YAML</text><text x=\"60\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">workflow file</text></g><line x1=\"110\" y1=\"60\" x2=\"135\" y2=\"60\" stroke=\"#C34113\" stroke-width=\"2\" marker-end=\"url(#h1)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"0.3s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.5s\" fill=\"freeze\"/><rect x=\"145\" y=\"25\" width=\"120\" height=\"70\" rx=\"8\" fill=\"#E95420\"/><text x=\"205\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Trigger</text><text x=\"205\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">push / PR / schedule</text></g><line x1=\"265\" y1=\"60\" x2=\"290\" y2=\"60\" stroke=\"#E95420\" stroke-width=\"2\" marker-end=\"url(#h2)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"0.7s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"0.9s\" fill=\"freeze\"/><rect x=\"300\" y=\"25\" width=\"110\" height=\"70\" rx=\"8\" fill=\"#238636\"/><text x=\"355\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Runner</text><text x=\"355\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">Hosted / self-hosted</text></g><line x1=\"410\" y1=\"60\" x2=\"435\" y2=\"60\" stroke=\"#238636\" stroke-width=\"2\" marker-end=\"url(#h1)\" stroke-dasharray=\"30\" stroke-dashoffset=\"30\"><animate attributeName=\"stroke-dashoffset\" from=\"30\" to=\"0\" dur=\"0.4s\" begin=\"1.1s\" fill=\"freeze\"/></line><g opacity=\"0\"><animate attributeName=\"opacity\" from=\"0\" to=\"1\" dur=\"0.4s\" begin=\"1.3s\" fill=\"freeze\"/><rect x=\"445\" y=\"25\" width=\"95\" height=\"70\" rx=\"8\" fill=\"#F0883E\"/><text x=\"492\" y=\"57\" text-anchor=\"middle\" fill=\"#fff\" font-size=\"13\" font-family=\"Ubuntu,sans-serif\" font-weight=\"700\">Job</text><text x=\"492\" y=\"75\" text-anchor=\"middle\" fill=\"rgba(255,255,255,0.75)\" font-size=\"10\" font-family=\"Ubuntu,sans-serif\">Steps execute</text></g></svg>",
  "sections": [
    {
      "title": "Workflow Structure",
      "description": "<p>A workflow is a YAML file in <code>.github/workflows/</code>. Top-level keys: <code>name</code>, <code>on</code> (trigger), <code>jobs</code>. Each job runs on a runner (<code>runs-on</code>) with <code>steps</code>. Steps can use <code>uses</code> (marketplace action) or <code>run</code> (shell command).</p><pre><code>name: CI\non: [push, pull_request]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm test</code></pre>"
    },
    {
      "title": "Triggers & Events",
      "description": "<p>Workflows trigger on events: <code>push</code>, <code>pull_request</code>, <code>schedule</code> (cron), <code>workflow_dispatch</code> (manual), <code>repository_dispatch</code> (API). Use <code>push:</code> with <code>branches:</code> and <code>paths:</code> filters for precise control. Activity types (e.g., <code>pull_request: [opened, synchronize]</code>) narrow triggers further.</p><pre><code>on:\n  push:\n    branches: [main]\n    paths: ['src/**']\n  schedule:\n    - cron: '0 6 * * 1'</code></pre>"
    },
    {
      "title": "Jobs & Steps",
      "description": "<p>Jobs run in parallel by default; <code>needs:</code> creates sequential dependencies. Steps share data via <code>$GITHUB_ENV</code> and <code>$GITHUB_OUTPUT</code>. Matrix strategies (<code>strategy.matrix</code>) run jobs across OS/version combinations. <code>if:</code> conditional syntax controls step execution.</p><pre><code>jobs:\n  lint:\n    runs-on: ubuntu-latest\n  test:\n    needs: lint\n    strategy:\n      matrix:\n        node: [18, 20]</code></pre>"
    },
    {
      "title": "Environment Variables",
      "description": "<p>Built-in env vars: <code>$GITHUB_WORKSPACE</code>, <code>$GITHUB_SHA</code>, <code>$GITHUB_REF</code>, <code>$GITHUB_EVENT_PATH</code> (event payload JSON). Secrets stored in Settings > Secrets are accessed via <code>$&#123;&#123; secrets.MY_SECRET &#125;&#125;</code>. Use environment files (<code>$GITHUB_ENV</code>, <code>$GITHUB_OUTPUT</code>) for inter-step communication.</p><pre><code>steps:\n  - name: Set env\n    run: echo \"MY_VAR=hello\" >> $GITHUB_ENV\n  - name: Use secret\n    run: deploy --token $&#123;&#123; secrets.DEPLOY_TOKEN &#125;&#125;</code></pre>"
    },
    {
      "title": "Marketplace Actions",
      "description": "<p>Pre-built actions from the marketplace: <code>actions/checkout</code>, <code>actions/setup-node</code>, <code>actions/cache</code>, <code>actions/upload-artifact</code>. Pin actions by commit SHA for security. Create custom actions as Docker containers, JavaScript, or composite run steps.</p><pre><code># Popular actions\n- uses: actions/checkout@v4\n- uses: actions/setup-node@v4\n- uses: actions/cache@v4\n  with:\n    path: ~/.npm\n    key: $&#123;&#123; runner.os &#125;&#125;-npm-$&#123;&#123; hashFiles('package-lock.json') &#125;&#125;</code></pre>"
    }
  ],
  "details": "<p>GitHub Actions uses a polling-based runner architecture. The hosted runner pool runs on Azure Standard_DS2_v2 VMs (2 cores, 7 GB RAM) with Ubuntu, Windows Server, or macOS. Each job gets a fresh VM with a clean filesystem. The <code>ACTIONS_RUNNER_DEBUG</code> secret enables step-level debug logging. Billing depends on included minutes (free tier: 2000 min/month for public repos, 500 min for private) — self-hosted runners are free. The <code>GITHUB_TOKEN</code> is auto-generated per job with scoped permissions and expires after the job. For security, use <code>permissions:</code> at the workflow or job level to restrict token access. Artifacts and logs are retained for 90 days by default. Actions support <code>workflow_call</code> (reusable workflows) and <code>composite actions</code> for DRY workflows.</p>",
  "cheatSheet": "<h4>Core Commands</h4><ul><li><code>.github/workflows/ci.yml</code> — workflow file location.</li><li><code>on: [push, pull_request]</code> — trigger on events.</li><li><code>jobs: test: runs-on: ubuntu-latest</code> — job definition.</li><li><code>strategy: matrix: node: [18, 20]</code> — run across versions.</li></ul><h4>Staff+ Patterns</h4><ul><li>Use <code>workflow_call</code> for reusable workflows — define a deployment workflow once, call it from all repos.</li><li>Pin actions by commit SHA (<code>actions/checkout@a81bbbf</code>) instead of version tag to prevent supply chain attacks.</li></ul><h4>Watch Outs</h4><ul><li><code>GITHUB_TOKEN</code> permissions are permissive by default. Always set <code>permissions: contents: read</code> at job level.</li><li>Matrix jobs that exceed 256 runners will be queued. Use concurrency limits to control parallel runs.</li></ul><h4>Interview Angle</h4><p>Netflix L6 asks about Actions architecture at scale: runner autoscaling, job scheduling, artifact caching. Explain the polling model and how the Actions coordinator manages the global job queue. Know the difference between hosted and self-hosted runners.</p>",
  "tags": [
    "github",
    "actions",
    "ci/cd"
  ],
  "datePublished": "2026-06-30",
  "lastModified": "2026-06-30"
}
